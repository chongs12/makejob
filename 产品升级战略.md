项目升级战略概要（供编程智能体理解与执行）
项目背景： 一个以Live2D角色为交互主体的AI求职辅助平台，包含论坛、题库（简答/选择/编程题）、AI模拟面试、学习陪伴聊天、后台管理及爬虫题卡生成。

全局战略： 利用大模型作为底层工具，在AI面试、题库和Live2D陪伴三大模块中，从“表面功能”转向“深度过程数据采集与个性化诊断”，建立数据闭环和竞争壁垒。

优先级1：AI面试深度诊断引擎（从评价答案到诊断思维）
核心目标： 将面试报告从笼统评分变为聚焦解题过程的精准能力诊断，尤其针对编程面试。

关键需求：

编程过程数据采集：在编程题编辑器内埋点，记录用户行为序列。

需采集事件：运行代码（含错误信息与行号）、提交代码、代码修改（记录修改位置与内容变化）、光标停留时长（超过阈值）。

输出：带时间戳的原始过程数据包。

基于过程的智能评价：生成面试报告时，将采集的过程数据作为LLM的 Prompt 上下文。

输入：题目信息、用户最终代码、过程数据包。

Prompt要点：要求模型分析用户在哪个知识点上反复试错、边界条件处理熟练度、与数据集中平均水平对比，并给出具体学习建议。

输出：结构化诊断报告，包含错因标签（如“状态定义不清”“边界条件生疏”）。

Live2D面试官的非语言反馈（进阶）：

通过摄像头采集用户表情/视线（需用户授权），当检测到“视线飘移”或“小动作增多”时，触发Live2D皱眉、侧头等动作。

根据答题连续正确/错误，动态调整Live2D的语速与追问风格（如进入压力模式）。

优先级2：从静态题库到动态能力知识图谱
核心目标： 让题库具备“知道用户为什么错”的诊断能力，并能自适应推送补偿练习。

关键需求：

智能题卡生成管线（改造爬虫模块）：

输入：爬取的最新面经/真题文本。

处理：利用LLM自动打上多维标签（知识点、难度、公司、题型）。

去重合并：识别同一题的不同表述并建立关联。

输出：结构化的“题卡”入库。

错因诊断与对症练习：

当用户提交错误答案（尤其是编程题），系统需调用LLM分析错误代码。

输入：题目描述、用户错误代码、测试用例报错信息。

要求：输出具体错因标签（如“动态规划的状态定义错误”），不得笼统。

反馈动作：根据错因标签，从题库中自动检索或由LLM生成几道针对性练习题（侧重同一薄弱点）推送给用户。

编程题AI教练模式：

Live2D角色具备主动提示能力。当编辑器检测到用户超30秒无操作或反复报错时，调用LLM观察当前代码与题目，生成一条不含完整答案的定向提示（如“第15行的循环终止条件会不会让数组越界？”）。

触发：定时/事件驱动 -> 调用LLM -> Live2D语音输出提示。

优先级3：Live2D从聊天陪伴升级为有长期记忆的导师
核心目标： 让Live2D角色拥有跨会话的个体学习档案记忆，并能主动规划学习任务，形成情感化绑定。

关键需求：

学习档案持久化：

需持久化存储：历史错题与错因标签、往期面试报告弱点项、用户设定的学习目标与日程。

每次对话开始时，自动加载该用户档案，作为LLM的长期记忆上下文。

主动提醒与规划：

日历/任务模块集成：用户设定学习计划后，Live2D在预定时间弹出并语音提醒，如“到昨天的链表复习时间了”。

学习内容衔接：下一次对话开启时，可主动说“上次你动态规划的状态定义总错，今天我们找道类似的题试试？”

社区成就的具象化表现：

将用户解题里程碑（如一道题击败99%的人、连续打卡）转化为Live2D的庆祝动作或特殊语音，并支持一键分享到“说说”或论坛。

给智能体的执行提示： 上述需求可以按模块逐步实现，但所有新功能的数据流都必须汇入用户学习档案，形成统一的数据闭环。开始编写时，应先从编程过程数据采集与错因诊断 Prompt 设计入手，这是最快能验证价值的切入点。