> ⚠️ **校正说明（2026-07-09 核验）**：本文档成于微服务改造早期（8 服务时代），正文数字与宣称已过时，使用前务必以下列为准：
> - **微服务数量**：当前 **15 个**微服务 + 1 个 BFF Gateway（不是 8 个）。服务清单见 `docs/phase-progress-and-run-guide.md`。
> - **Proto / RPC**：proto-first 设计，gRPC 方法 **100+ 个**（不是 80+）。
> - **Bridge 模式**：已在 Phase 6 **完全移除**，Gateway 为纯 gRPC 代理（不再有 bridge.Runtime 双模切换）。
> - **可观测性**：`Prometheus + Grafana + Jaeger 全链路` 是**未落地的规划**，属 Phase 7 待办；当前仅有 zap 结构化日志 + 请求 ID。正文相关宣称请勿照搬。
> - **代码路径**：正文 `backend/internal/...` 已随微服务化迁移到 `app/<service>/...`（单体归档于 `docs/backend/`）。
> - 正文量化指标表、架构图、讲述脚本中的数字仍为旧值，正式用于面试前请按上述校正刷新。

  面试专用·MakeJob 项目深度剖析与展示指南

  ---
  1. 项目速览（电梯演讲）

  一句话概括：

  ▎ 为解决技术求职者「刷题零散、面试缺乏真实感、学习路径无数据驱动」的核心痛点，我主导设计并实现了 MakeJob —— 一个 AI
  ▎ 驱动的全链路面试模拟与学习成长平台，通过「多模态实时面试 + RAG 知识增强 +
  ▎ 学习档案闭环」达成了从刷题到面试到复盘的完整学习飞轮。

  项目基本信息：

  ┌──────────┬───────────────────────────────────────────────────────────────────────────┐
  │   维度   │                                   详情                                    │
  ├──────────┼───────────────────────────────────────────────────────────────────────────┤
  │ 项目类型 │ AI 驱动的 ToC SaaS 平台（技术面试准备）                                   │
  ├──────────┼───────────────────────────────────────────────────────────────────────────┤
  │ 我的角色 │ 技术负责人 / 全栈主导开发者                                               │
  ├──────────┼───────────────────────────────────────────────────────────────────────────┤
  │ 团队规模 │ 【待补充 —— 推断为 1-3 人精干团队，因代码风格高度统一、架构决策高度集中】 │
  ├──────────┼───────────────────────────────────────────────────────────────────────────┤
  │ 项目周期 │ 【待补充 —— 从 git 历史看，至少经历 4+ 个大版本迭代】                     │
  └──────────┴───────────────────────────────────────────────────────────────────────────┘

  从代码中提取的量化指标：

  ┌────────────────┬──────────────────────────────────────────────────────────────────┬─────────────────────────────┐
  │      指标      │                               数值                               │            来源             │
  ├────────────────┼──────────────────────────────────────────────────────────────────┼─────────────────────────────┤
  │ 微服务数量     │ 8 个 + 1 个 BFF Gateway                                          │ app/ 目录下 8 个服务        │
  ├────────────────┼──────────────────────────────────────────────────────────────────┼─────────────────────────────┤
  │ Proto RPC      │ 80+ 个                                                           │ api/ 下 10 个 proto 文件    │
  │ 方法数         │                                                                  │                             │
  ├────────────────┼──────────────────────────────────────────────────────────────────┼─────────────────────────────┤
  │ 数据库模型数   │ 30+ 张表                                                         │ backend/internal/model/     │
  │                │                                                                  │ 目录                        │
  ├────────────────┼──────────────────────────────────────────────────────────────────┼─────────────────────────────┤
  │ AI Agent 类型  │ 6                                                                │ backend/internal/ai/        │
  │                │ 种（Interview/Plan/Companion/Quiz/ResumeParser/Live2DDirector）  │ 接口定义                    │
  ├────────────────┼──────────────────────────────────────────────────────────────────┼─────────────────────────────┤
  │ MQ 任务队列    │ 8 条                                                             │ pkg/mq/message.go 队列常量  │
  ├────────────────┼──────────────────────────────────────────────────────────────────┼─────────────────────────────┤
  │ 测试文件数     │ 52 个                                                            │ 项目内 *_test.go 文件统计   │
  ├────────────────┼──────────────────────────────────────────────────────────────────┼─────────────────────────────┤
  │ 限流层级       │ 3 级（default 100/s, strict 1/s, public 20/s）                   │ middleware/ratelimit.go     │
  ├────────────────┼──────────────────────────────────────────────────────────────────┼─────────────────────────────┤
  │ TTS 提供商     │ 3 家（Volcengine / MiniMax / Xiaomi Mimo）                       │ internal/tts/ 目录          │
  ├────────────────┼──────────────────────────────────────────────────────────────────┼─────────────────────────────┤
  │ Live2D         │ 6 个                                                             │ live2d-src/ 目录            │
  │ 角色模型       │                                                                  │                             │
  ├────────────────┼──────────────────────────────────────────────────────────────────┼─────────────────────────────┤
  │ 前端页面模块   │ Web 5 个 + Admin 10 个                                           │ frontend-react/apps/        │
  └────────────────┴──────────────────────────────────────────────────────────────────┴─────────────────────────────┘

  ---
  2. 系统架构深度解析

  2.1 架构分层图

  ┌─────────────────────────────────────────────────────────────────┐
  │                        前端层 (Frontend)                         │
  │  React 19 + Vite 7 + TypeScript + TanStack Router/Query         │
  │  ┌──────────────┐  ┌──────────────┐  ┌────────────────────────┐ │
  │  │  Web App      │  │  Admin App   │  │ @makejob/api-client    │ │
  │  │ (用户端)      │  │ (管理后台)    │  │ @makejob/shared-types  │ │
  │  └──────────────┘  └──────────────┘  └────────────────────────┘ │
  │  Live2D (pixi.js) | Monaco Editor | PDF.js | PCM Stream Player  │
  └────────────────────────────┬────────────────────────────────────┘
                               │ HTTP/REST + WebSocket
  ┌────────────────────────────▼────────────────────────────────────┐
  │                     网关层 (BFF Gateway)                          │
  │  Gin HTTP Server (:8082) + JWT Auth + CORS + gRPC Proxy          │
  │  ┌─────────────────────────────────────────────────────────────┐ │
  │  │  bridge.Runtime 双模切换:                                    │ │
  │  │  · 纯 gRPC 代理模式 → 转发到微服务                           │ │
  │  │  · Bridge 模式 → 直接调用 monolith 后端能力                   │ │
  │  └─────────────────────────────────────────────────────────────┘ │
  └────────┬──────────┬──────────┬──────────┬──────────┬────────────┘
           │ gRPC     │ gRPC     │ gRPC     │ gRPC     │ gRPC
  ┌────────▼──┐ ┌─────▼────┐ ┌──▼───────┐ ┌▼────────┐ ┌▼────────┐
  │ user      │ │ question │ │interview │ │ growth  │ │ admin   │
  │ :9004     │ │ :9002    │ │ :9003    │ │ :9005   │ │ :9006   │
  │ 9 RPCs    │ │ 16 RPCs  │ │ 14 RPCs  │ │ 8 RPCs  │ │ 50+ RPCs│
  └───────────┘ └──────────┘ └──────────┘ └─────────┘ └─────────┘
  ┌───────────┐ ┌──────────────────────┐
  │community  │ │ learning_archive     │
  │ :9007     │ │ :9008                │
  └───────────┘ └──────────────────────┘

  每个微服务遵循 Kratos DDD 四层架构:
  cmd/server → service/ → biz/ → data/
                          ↕
                     server/ (gRPC + MQ transport)

  ┌─────────────────────────────────────────────────────────────────┐
  │                     基础设施层 (Infrastructure)                    │
  │  ┌──────────┐ ┌────────┐ ┌──────────┐ ┌────────┐ ┌──────────┐  │
  │  │PostgreSQL│ │ Redis  │ │ RabbitMQ │ │ Milvus │ │  etcd    │  │
  │  │ :5434    │ │ :6384  │ │ :5672    │ │:19530  │ │ :2379    │  │
  │  └──────────┘ └────────┘ └──────────┘ └────────┘ └──────────┘  │
  │  ┌──────────┐ ┌────────────────┐ ┌──────────────────────────┐  │
  │  │  Piston  │ │ Volcengine ASR │ │ Prometheus+Grafana+Jaeger│  │
  │  │ 代码执行  │ │ TTS / WebSocket│ │      可观测性全链路       │  │
  │  └──────────┘ └────────────────┘ └──────────────────────────┘  │
  └─────────────────────────────────────────────────────────────────┘

  2.2 核心链路时序图

  链路一：AI 模拟面试全流程（简历驱动模式）

  用户          Gateway        Interview服务       AI服务(Agent)      MQ(RabbitMQ)     LearningArchive
   │               │                │                   │                  │                │
   │ 上传简历+选行业 │                │                   │                  │                │
   │──────────────>│                │                   │                  │                │
   │               │  CreateInterview                   │                  │                │
   │               │───────────────>│                   │                  │                │
   │               │                │ 创建面试记录       │                  │                │
   │               │                │ (status=preparing)│                  │                │
   │               │                │                   │                  │                │
   │               │                │ 发送MQ:简历解析    │                  │                │
   │               │                │──────────────────────────────────>│                  │
   │               │  返回面试ID     │                   │                  │                │
   │               │<───────────────│                   │                  │                │
   │  轮询面试状态   │                │                   │                  │                │
   │──────────────>│                │                   │                  │                │
   │               │                │   Worker消费MQ     │                  │                │
   │               │                │<──────────────────────────────────│                  │
   │               │                │   调用ResumeParser  │                  │                │
   │               │                │──────────────────>│                  │                │
   │               │                │   返回结构化画像    │                  │                │
   │               │                │<──────────────────│                  │                │
   │               │                │   更新status=ongoing                 │                │
   │               │                │   调用InterviewAgent.StartInterview  │                │
   │               │                │──────────────────>│                  │                │
   │               │                │   返回首题+sessionId│                 │                │
   │               │                │<──────────────────│                  │                │
   │               │                │                   │                  │                │
   │  提交回答      │                │                   │                  │                │
   │──────────────>│  SubmitAnswer  │                   │                  │                │
   │               │───────────────>│                   │                  │                │
   │               │                │ 保存InterviewMessage                  │                │
   │               │                │ 调用Agent.EvaluateAnswer              │                │
   │               │                │──────────────────>│                  │                │
   │               │                │ 写入LearningArchive│                  │                │
   │               │                │──────────────────────────────────────────────────>│
   │               │                │ 调用Agent.GetNextQuestion             │                │
   │               │                │──────────────────>│                  │                │
   │               │  返回反馈+下一题│                   │                  │                │
   │               │<───────────────│                   │                  │                │
   │               │                │                   │                  │                │
   │  ...循环N题... │                │                   │                  │                │
   │               │                │                   │                  │                │
   │  结束面试      │                │                   │                  │                │
   │──────────────>│  FinishInterview                  │                  │                │
   │               │───────────────>│                   │                  │                │
   │               │                │ 发送MQ:报告生成    │                  │                │
   │               │                │──────────────────────────────────>│                  │
   │               │                │                   │  Worker:调用Agent.GenerateReport  │
   │               │                │                   │<─────────────────│                │
   │               │                │                   │  返回报告        │                │
   │               │                │<──────────────────────────────────│                  │
   │               │                │ 更新status=completed                 │                │
   │               │  返回报告       │                   │                  │                │
   │               │<───────────────│                   │                  │                │

  关键设计决策点（标注★）：
  - ★ status=preparing 状态机设计：简历解析为异步，用户无需等待 AI 处理完成
  - ★ LearningArchive 每次答题后自动写入错题标签/强项标签，驱动后续个性化推荐
  - ★ 报告生成走 MQ 异步，避免 AI 长耗时阻塞用户请求
  - ★ Interview Session 绑定到创建时的 Provider，中途改配置不影响进行中的面试

  链路二：AI 配置热重载

  Admin前端        Gateway       Admin服务        RuntimeManager      AI Client
   │                 │              │                  │                  │
   │ 修改AI模型配置   │              │                  │                  │
   │────────────────>│  UpdateConfig│                  │                  │
   │                 │─────────────>│                  │                  │
   │                 │              │ 写入admin_configs │                  │
   │                 │              │ 表               │                  │
   │                 │              │                  │                  │
   │                 │              │  下次请求时:       │                  │
   │                 │              │                  │ 计算config指纹    │
   │                 │              │                  │ (MD5 of configs) │
   │                 │              │                  │                  │
   │                 │              │                  │ 指纹变化?         │
   │                 │              │                  │ 是 → 重建全部Agent│
   │                 │              │                  │─────────────────>│
   │                 │              │                  │ 新Client生效      │
   │                 │              │                  │                  │
   │                 │              │  已有Session:     │                  │
   │                 │              │                  │ 绑定旧Provider    │
   │                 │              │                  │ (sync.Map存储)    │

  2.3 技术栈全景表

  ┌───────────────┬─────────────────────────────────┬───────────────────────────────────────────────────────────────┐
  │     分类      │            技术名称             │                           选型理由                            │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 后端语言      │ Go 1.25                         │ 高并发、编译型、适合微服务；推断：团队主力语言，Go 生态成熟   │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ HTTP 框架     │ Gin v1.12                       │ 轻量高性能，社区生态最大；Gateway 和 Monolith 均使用          │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 微服务框架    │ Kratos v2.8 (B站开源)           │ Go 原生、DDD 四层标准化、内置 gRPC/HTTP 双协议、Wire DI       │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ RPC 协议      │ gRPC + Protobuf                 │ 强类型 API 契约、高性能序列化、Proto-first 设计               │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ API 定义工具  │ Buf v2                          │ 替代 protoc，统一 lint/format/generate，CI 友好               │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ ORM           │ GORM v1.31                      │ Go 最流行 ORM，支持 AutoMigrate、关联查询、事务               │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 数据库        │ PostgreSQL                      │ 开源关系型首选，JSON 支持好，适合复杂查询                     │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 缓存          │ Redis (go-redis)                │ 分布式限流、Session、缓存，Lua 脚本保证原子性                 │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 消息队列      │ RabbitMQ (amqp091-go)           │ 成熟可靠、支持死信/延迟/重试、管理界面友好                    │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 向量数据库    │ Milvus v2.5.8                   │ 开源向量搜索、支持亿级向量、RAG 核心组件                      │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ AI 框架       │ CloudWeGo Eino (字节开源)       │ 字节内部验证、与 Ark 模型深度集成、支持 Chat/Stream           │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ AI 模型后端   │ Volcengine Ark / MiniMax        │ 国内大模型服务，延迟低，合规                                  │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 语音合成(TTS) │ Volcengine + MiniMax + Xiaomi   │ 三家 provider 工厂模式，可热切换                              │
  │               │ Mimo                            │                                                               │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 语音识别(ASR) │ Volcengine                      │ 实时语音对话场景，端到端延迟低                                │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 实时通信      │ WebSocket (Volcengine Realtime  │ 实时语音面试、Live2D 指令下发                                 │
  │               │ Dialog)                         │                                                               │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 代码执行      │ Piston (自托管)                 │ 多语言支持(Go/Python/JS/Java/C++)、沙箱隔离、networking       │
  │               │                                 │ disabled                                                      │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 虚拟形象      │ Live2D (pixi.js + Cubism 4)     │ 2D 动画角色、表情/动作可编程控制                              │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 权限控制      │ Casbin v2                       │ RBAC 模型、策略可热更新、admin bypass 设计                    │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 认证          │ JWT (HS256) + gRPC Interceptor  │ 无状态认证、白名单公开方法、服务间调用 client interceptor     │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 依赖注入      │ Wire (编译时)                   │ 编译时安全、零反射开销、Kratos 推荐                           │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 服务发现      │ etcd v3                         │ 分布式一致性、lease/keepalive 自动注销                        │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 可观测性-指标 │ Prometheus + Grafana            │ HTTP/AI/WebSocket/Go Runtime 四维指标                         │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 可观测性-链路 │ OpenTelemetry + Jaeger          │ OTLP gRPC 导出、可配置采样率                                  │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 可观测性-日志 │ Zap + Lumberjack                │ 结构化 JSON 日志、自动切割轮转                                │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 配置管理      │ Viper (YAML + env)              │ 多格式支持、环境变量覆盖、热加载                              │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 前端框架      │ React 19 + TypeScript 5.9       │ 最新 React、类型安全                                          │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 前端构建      │ Vite 7                          │ 极速 HMR、ESM 原生                                            │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 前端路由      │ TanStack Router                 │ 类型安全路由、文件路由支持                                    │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 前端状态      │ Zustand + TanStack Query        │ 轻量客户端状态 + 服务端缓存分离                               │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 前端编辑器    │ Monaco Editor                   │ 代码题在线编辑、语法高亮                                      │
  ├───────────────┼─────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 前端 PDF      │ PDF.js                          │ 简历预览/解析                                                 │
  └───────────────┴─────────────────────────────────┴───────────────────────────────────────────────────────────────┘

  ---
  3. 个人贡献矩阵（主导者视角）

  基于代码复杂度、模块核心程度、git 提交信息、文档深度和架构痕迹推断：

  ┌───────────┬─────────────────────────────────┬─────────────────────────────────────┬───────────────────────────┐
  │ 模块名称  │          我的主导工作           │            核心技术难点             │         直接成果          │
  ├───────────┼─────────────────────────────────┼─────────────────────────────────────┼───────────────────────────┤
  │ AI        │ 设计并实现指纹热重载、Provider  │                                     │ 6 种 AI Agent             │
  │ Runtime   │ 链（fallback+命名追踪）、Sessio │ 配置变更无感切换 +                  │ 可热切换模型/provider，52 │
  │ 动态引擎  │ n 绑定、6 种 Agent 接口抽象     │ 进行中会话不中断的并发安全          │  个测试文件中 AI 相关 15+ │
  │           │                                 │                                     │  个                       │
  ├───────────┼─────────────────────────────────┼─────────────────────────────────────┼───────────────────────────┤
  │           │ 从 Gin Monolith 拆分为 8 个     │ Go 模块边界下复用 monolith          │                           │
  │ 微服务架  │ Kratos 微服务 + BFF             │ 能力、Proto-first 80+ RPC           │ 8 服务 + 12 个 proto 文件 │
  │ 构迁移    │ Gateway，设计 Bridge            │ 设计、双模 Gateway                  │  + 完整 Makefile 构建链   │
  │           │ 模式实现渐进迁移                │                                     │                           │
  ├───────────┼─────────────────────────────────┼─────────────────────────────────────┼───────────────────────────┤
  │ 实时语音  │ 集成 Volcengine WebSocket       │ 多模态编排（TTS+ASR+Live2D+字幕）、 │ 支持文本/语音双链路面试， │
  │ 面试系统  │ 实时对话、ASR/TTS 多 provider   │ Event 502 RAG 外部注入、PCM         │ 6 个 Live2D 角色          │
  │           │ 工厂、Live2D 指令生成           │ 流式播放                            │                           │
  ├───────────┼─────────────────────────────────┼─────────────────────────────────────┼───────────────────────────┤
  │ RAG 知识  │ 设计 Milvus + Eino Embedding +  │ 向量检索与面试上下文融合、MQ        │ 支持语义搜索/同义词理解/  │
  │ 增强系统  │ MQ 异步同步的完整 RAG           │ 驱动的增量同步、中文语义搜索优化    │ 岗位匹配，Postman         │
  │           │ 管线，集成到面试 prompt 增强    │                                     │ 测试集完备                │
  ├───────────┼─────────────────────────────────┼─────────────────────────────────────┼───────────────────────────┤
  │ 学习飞轮  │ 设计 LearningArchive +          │ 错题标签频率排序→训练焦点→智能出题  │ 个性化推荐 + AI 学习计划  │
  │ 闭环      │ FocusSignal + MistakeTopic      │ 的数据闭环、阶段蓝图系统            │ + 智能调整                │
  │           │ 的数据驱动推荐系统              │                                     │                           │
  ├───────────┼─────────────────────────────────┼─────────────────────────────────────┼───────────────────────────┤
  │ 可观测性  │ 四阶段渐进式建设：请求追踪→Prom │ 全链路 X-Request-ID、6 种 Agent     │ Grafana Dashboard +       │
  │ 体系      │ etheus 指标→AI 日志完善→Token   │ 的结构化日志、AIProvider            │ Jaeger 链路 + Prometheus  │
  │           │ 用量采集                        │ 接口变更返回 TokenUsage             │ 四维指标                  │
  ├───────────┼─────────────────────────────────┼─────────────────────────────────────┼───────────────────────────┤
  │ 异步任务  │ 设计 RabbitMQ Topic/DLX/Retry   │ 8 种任务类型的差异化重试策略、Async │ 8 条队列、3-5             │
  │ 架构      │ 拓扑、幂等 key、状态机（queued→ │ DispatchOption 优雅降级             │ 次重试、20-45s 延迟梯度   │
  │           │ running→succeeded/failed/dead） │                                     │                           │
  └───────────┴─────────────────────────────────┴─────────────────────────────────────┴───────────────────────────┘

  ---
  4. 项目亮点深度剖析（STAR-L 法则）

  亮点一：AI Runtime 动态引擎——零重启的 AI 配置热重载

  情境 (Situation)： 平台需要支持多种 AI 模型（Eino/Ark、MiniMax 等）和 6 种不同场景的
  Agent，运营人员需要频繁调整模型参数、切换 provider、修改 prompt 模板。传统的重启生效方式在生产环境不可接受。

  任务 (Task)： 设计一个支持配置热重载的 AI Runtime，做到：改配置后新请求立即用新模型，但进行中的面试会话不受影响。

  行动 (Action)：
  - 在 backend/internal/ai/runtime/dynamic_client.go 中实现 RuntimeManager，每次调用时计算 admin_configs 表的 MD5
  指纹，指纹变化则触发整个 AI Client 重建（含所有 Agent 和 Provider）
  - 使用 sync.Map 实现 Session 绑定（backend/internal/ai/runtime/interview_agent.go）：面试开始时将 sessionID 绑定到当前
   Provider 实例，后续该会话的所有调用都路由到同一个 Provider
  - 设计 Provider 链模式（backend/internal/ai/runtime/builder.go）：providerWithFallback（主备切换）→
  namedProvider（追踪实际使用的模型）→ unavailableProvider（兜底错误）
  - Prompt 模板支持 {{variable}} 替换，按 industry + scene 从数据库查询，fallback 到内置默认值

  结果 (Result)：
  - 配置变更延迟 < 1 次请求周期（即下一个请求即生效）【待量化：具体 ms 级延迟】
  - 52 个测试文件中 AI Runtime 相关 15+ 个，覆盖 builder、dynamic_client、各 agent 的核心路径
  - 支持 3+ 个 AI provider 的无缝切换，无需重启服务

  学习与成长 (Learning)： 指纹 + 缓存失效是解决「热重载 vs 稳定性」矛盾的通用模式。关键
  insight：「全局配置可以热更新，但有状态会话必须绑定到创建时的实例」——这个 Session 绑定设计后续被复用到 TTS provider
  选择上。

  ---
  亮点二：渐进式微服务迁移——Bridge 模式解决 Go 模块边界难题

  情境 (Situation)： 项目从 Gin 单体演进到需要拆分微服务，但 Go
  的模块系统不允许循环依赖，且微服务化不可能一蹴而就——AI、RAG、Live2D、Scraper 等复杂能力短期内无法全部迁移。

  任务 (Task)： 设计一个渐进式迁移方案，使得 Gateway 可以同时代理新微服务和复用旧单体能力，且对前端完全透明。

  行动 (Action)：
  - 在 backend/bridge/ 中设计 bridge.Runtime，将整个后端打包为可嵌入的运行时（runtime.go），暴露 RegisterGatewayRoutes
  方法
  - Gateway（app/gateway/internal/proxy/handler.go，~3100 行）实现双模切换：有 bridge 时委托给 monolith，无 bridge 时走
  gRPC 代理
  - 每个微服务严格遵循 Kratos DDD 四层：cmd/server → service → biz → data，biz 层定义接口、data 层实现，解耦彻底
  - 使用 Buf 生成 Proto-first API（12 个 proto 文件，80+ RPC），确保接口契约一致
  - MQ Consumer 实现为 kratos/transport.Server，与 gRPC Server 在同一进程运行

  结果 (Result)：
  - 8 个微服务 + 1 个 BFF Gateway 完整搭建，端口独立分配
  - 前端零改动完成迁移——Gateway 对外 API 不变
  - 2026-06-02 的 bridge 集成记录（docs/microservice-migration.md）验证了方案可行性

  学习与成长 (Learning)： Go 微服务迁移的核心不是「拆代码」而是「拆依赖」。Bridge
  模式本质上是一个反腐败层（ACL），让新旧系统可以共存。Proto-first 的好处远超类型安全——它是团队协作的契约。

  ---
  亮点三：多模态实时面试——从文本到语音到 Live2D 的全链路编排

  情境 (Situation)： 传统面试系统只有文字交互，缺乏真实面试的压力感和沉浸感。需要实现语音面试 + 虚拟面试官 + 实时 RAG
  知识增强的完整体验。

  任务 (Task)： 打通 ASR→AI→TTS→Live2D→字幕的全链路实时数据流，且支持文本/语音双模式。

  行动 (Action)：
  - 设计双链路架构（docs/ai面试功能技术要点.md）：文本模式走 HTTP 同步，语音模式走 WebSocket 实时
  - ASR/TTS 均采用工厂模式（internal/asr/factory/、internal/tts/factory/），支持 Volcengine/MiniMax/Mimo 三家 provider
  热切换
  - Live2D 指令生成（internal/ai/runtime/live2d_director.go）：AI 输出结构化 JSON
  控制表情混合（expression_mix）、动作（motion_key）、嘴型（mouth_open），前端解析后驱动 Cubism 4
  - RAG 通过 Volcengine Event 502 注入实时语音流（docs/milestone-rag-interview-integration.md）：面试过程中 AI
  检索相关知识后以外部事件形式注入 TTS 流
  - 编码题过程事件采集（interview_coding_support.go）：代码快照、运行结果、长时间停顿 → CodingQuestionDiagnosis

  结果 (Result)：
  - 支持 6 个 Live2D 角色模型，每个有 7-17 个表情 + 物理模拟
  - 文本/语音双模式并行，用户可无缝切换 【待量化：端到端延迟数据】
  - TTS 超时 fallback 机制（Live2D 指令生成超时时使用默认表情）

  学习与成长 (Learning)： 多模态系统的核心难点不是各组件的对接，而是时序编排和降级策略。Event 502 的 RAG
  注入方案是与火山引擎文档深度研究后的创新——不是在 prompt 里塞 context，而是在语音流中实时注入。

  ---
  亮点四：数据驱动的学习飞轮——LearningArchive 闭环推荐系统

  情境 (Situation)：
  市面上的刷题产品推荐逻辑多基于「简单统计」（如错题重练），缺乏对用户学习信号的深度理解和跨场景联动。

  任务 (Task)： 设计一个跨练习、面试、计划三个场景的数据闭环，实现真正的个性化推荐。

  行动 (Action)：
  - 设计 LearningArchiveEntry
  模型（internal/model/learning_archive_entry.go）：每次练习答题、面试编码、计划反馈后自动写入，包含
  mistake_tags、strength_tags、suggestions
  - 实现 FocusSignal 系统（service/focus_signal_support.go）：从 archive 条目中提取错题标签频率，映射为
  MistakeTopicCard，按频率排序
  - 学习计划生成时注入 FocusSignal（service/plan_service.go）：构建 UserProfile 时包含弱项主题，AI
  生成的计划天然针对用户短板
  - 推荐系统（service/question_service.go → GetPracticeRecommendations）：Focus
  标签扩展为关键词搜索题目，每条推荐附带来源归因、优先级解释和推荐理由
  - 阶段蓝图系统（service/plan_blueprint_support.go）：学习计划的时间窗口→任务类型→退出条件的版本化映射，支持智能调整时
  尊重原始学习进度

  结果 (Result)：
  - 练习推荐从「随机出题」升级为「数据驱动的弱项攻坚」
  - 学习计划支持 AI 自动调整，且调整时保留完整上下文（已完成任务+诊断数据+阶段蓝图） 【待量化：用户留存/完课率提升数据】
  - Companion 服务也能利用 archive 数据丰富对话上下文

  学习与成长 (Learning)： 数据闭环的关键不是「记录数据」而是「设计信号」。LearningArchive 的 mistake_tags/ strength_tags
   设计让下游的推荐、计划、伴学三个场景都能消费同一份信号，而不是各自维护独立的用户画像。

  ---
  亮点五：分布式 MQ 任务系统——幂等+重试+死信的生产级设计

  情境 (Situation)： 平台有 8 种异步任务（简历解析、报告生成、RAG
  同步等），每种任务的耗时、失败率、重试策略都不同，且需要保证幂等性。

  任务 (Task)： 设计一个统一的 MQ 任务框架，支持差异化重试、死信兜底、幂等去重、优雅降级。

  行动 (Action)：
  - 在 pkg/mq/ 中设计三层 Exchange 拓扑（topology.go）：Topic Exchange（正常路由）→ Retry Exchange（带延迟）→
  DLX（死信兜底）
  - 每种任务类型独立配置重试次数（3-5 次）和延迟（20-45 秒），在 message.go 中用 QueueSpec 定义
  - 所有消息携带幂等 key，Consumer 端去重（consumer.go）
  - AsyncDispatchOption 模式：Service 层接受可选的 Publisher 参数，有 Publisher 走 MQ，无 Publisher
  降级为同步执行——解决了 MQ 不可用时的优雅降级
  - MQ Consumer 实现 kratos/transport.Server 接口，与 gRPC Server 统一生命周期管理

  结果 (Result)：
  - 8 条队列稳定运行，覆盖面试全流程和管理后台流水线
  - RabbitMQ 不可用时自动降级为同步，用户无感知 【待量化：消息处理吞吐量/延迟数据】
  - Publisher Confirms 保证消息不丢失

  学习与成长 (Learning)： MQ 系统设计的核心是「不要信任消息一定成功」。幂等 key + 死信队列 +
  差异化重试三板斧缺一不可。AsyncDispatchOption 模式是意外收获——它让「MQ
  是可选的」成为架构级约束，极大提升了系统的可测试性。

  ---
  5. 面试讲述脚本

  5.1 一分钟极速版

  ▎ 我主导开发了一个叫 MakeJob 的 AI 面试模拟平台，核心是帮技术求职者从刷题到模拟面试到复盘形成完整闭环。
  ▎
  ▎ 技术上，后端从 Gin 单体演进到 8 个 Kratos 微服务，用 gRPC 通信、RabbitMQ 做异步任务、Milvus 做 RAG
  ▎ 向量检索。最有挑战的是 AI Runtime 引擎——我设计了一个指纹热重载机制，运营改 AI
  ▎ 配置后新请求立即生效，但进行中的面试不受影响。同时实现了语音面试+Live2D 虚拟面试官的多模态编排，通过火山引擎
  ▎ WebSocket 实时语音+AI 结构化指令控制 Live2D 表情动作。
  ▎
  ▎ 前端是 React 19 + Vite 的 monorepo，包含用户端和管理后台。整个项目 52 个测试文件、30+ 张数据表、80+ 个 gRPC
  ▎ 接口，可观测性上做了 Prometheus 四维指标 + Jaeger 全链路追踪。

  5.2 三分钟深度版

  ▎ 我来介绍下 MakeJob
  ▎ 这个项目。背景是这样的：技术求职者刷题零散、面试缺乏真实感、学习路径没有数据支撑。所以我设计了一个 AI
  ▎ 驱动的全链路平台，覆盖刷题、模拟面试、学习计划、社区交流四个场景。
  ▎
  ▎ 架构演进上，项目经历了从 Gin 单体到 Kratos 微服务的完整迁移。拆了 8
  ▎ 个服务——user、question、interview、growth、admin、community、learning_archive，加一个 BFF Gateway。迁移最大的挑战是
  ▎ Go 的模块边界问题，单体里 AI、RAG、Live2D 这些复杂能力短期内不可能全迁移，所以我设计了一个 Bridge
  ▎ 模式——把整个单体打包为可嵌入的 runtime，Gateway 有 bridge 时走单体，没有时走 gRPC 代理，对前端完全透明。12 个 proto
  ▎ 文件定义了 80+ 个 RPC 方法，用 Buf 做 Proto-first 的代码生成。
  ▎
  ▎ AI 系统是最有技术深度的部分。我设计了一个动态 Runtime 引擎，支持 3+ 个 AI provider（字节 Eino、MiniMax、火山引擎）和
  ▎  6 种场景 Agent（面试、计划、伴学、题解、简历解析、Live2D 指令）。核心是配置指纹热重载——每次调用时计算数据库配置的
  ▎ MD5，变了就重建全部 Agent，但通过 sync.Map 做 Session
  ▎ 绑定，进行中的面试不会被中断。这个设计解决了一个真实痛点：运营需要频繁调模型参数，但用户正在面试时不能断。
  ▎
  ▎ 多模态面试是最有业务亮点的部分。文本模式走 HTTP，语音模式走 Volcengine WebSocket 实时对话。ASR/TTS
  ▎ 都是工厂模式，三家 provider 可热切换。最有创新的是 Live2D 编排——AI 输出结构化 JSON
  ▎ 控制虚拟面试官的表情、动作、嘴型，前端用 pixi.js 驱动 Cubism 4 渲染。还有一个细节：RAG 知识增强不是简单塞到 prompt
  ▎ 里，而是通过火山引擎的 Event 502 机制在语音流中实时注入。
  ▎
  ▎ 数据驱动的推荐闭环也是我重点设计的。每次练习、面试、计划反馈都会自动写入
  ▎ LearningArchive，系统从中提取错题标签频率生成
  ▎ FocusSignal，再驱动个性化推荐和学习计划生成。这让练习推荐从「随机出题」变成了「弱项攻坚」。
  ▎
  ▎ 可观测性做了四阶段渐进式建设：X-Request-ID 请求追踪→Prometheus 四维指标→6 种 Agent 的结构化 AI 日志→Token
  ▎ 用量采集。整套 Prometheus + Grafana + Jaeger 的监控栈用 Docker Compose 一键拉起。
  ▎
  ▎ 这个项目让我对AI 应用工程化有了很深的理解——不是调 API
  ▎ 就行，而是要解决配置热重载、会话隔离、多模态编排、数据闭环这些工程问题。（埋钩子：面试官可以追问 AI Runtime
  ▎ 的具体实现或多模态编排的细节）

  ---
  6. 潜在追问与应答策略

  Q1：为什么选择 Kratos 而不是 Go-Micro、go-zero 或自研框架？

  高分要点：
  - Kratos 是 B 站内部大规模验证过的，DDD 四层架构（biz/data/service/server）强制业务逻辑与基础设施解耦
  - 内置 gRPC + HTTP 双协议、Wire DI、Config、Discovery，不需要拼凑各种库
  - MQ Consumer 实现为 transport.Server 接口，与 gRPC Server 统一生命周期——这点 go-zero 做不到
  - 对比 go-zero：go-zero 偏重 codegen，Kratos 更偏架构约束；我们已有大量存量代码，Kratos 的渐进式接入更友好

  Q2：Bridge 模式和直接用 API Gateway 反代有什么区别？为什么不用 Istio/Envoy？

  高分要点：
  - 核心区别是进程内复用——Bridge 模式是把单体编译进 Gateway 进程，走函数调用而非网络请求，零额外延迟
  - Istio/Envoy 解决的是网络层路由，但我们的痛点是 Go 模块边界——单体的 internal/ 包无法被外部 import，必须通过 bridge
  打包暴露
  - 这是渐进式迁移方案，不是终态——随着微服务能力逐步完善，Bridge 会逐渐退化直到移除
  - 引用文件：backend/bridge/runtime.go 中的 RegisterGatewayRoutes 方法

  Q3：AI 配置热重载的指纹机制具体怎么实现的？有没有并发问题？

  高分要点：
  - RuntimeManager 维护一个 configFingerprint（MD5 of admin_configs 表所有配置项）
  - 每次 AI 调用前 compute fingerprint，与缓存比较，不同则触发 rebuild
  - 并发安全：用 sync.RWMutex 保护 fingerprint 检查和 Client 替换——读锁检查指纹，写锁重建 Client
  - Session 绑定用 sync.Map 存储 sessionID → provider，面试开始时绑定，面试结束后清理
  - 测试覆盖：dynamic_client_test.go 验证了指纹变化触发重建、Session 绑定不被覆盖等场景

  Q4：RAG 的向量检索质量怎么保证？中文语义搜索效果如何？

  高分要点：
  - 使用字节 doubao-embedding-large-text 模型生成向量，维度高、中文语义理解好
  - Milvus v2.5.8 做向量存储，支持余弦相似度/L2/IP 多种距离度量
  - 数据同步通过 RabbitMQ 异步——题目变更→MQ→Worker→Embedding→Milvus，增量更新
  - 中文优化：问题构建器（rag/question_builder.go）会将题目结构化信息拼接为语义丰富的文本再 embedding
  - 已知不足（docs/rag-todo.md）：P2 级别的中文截断风险（byte-based truncation）、topK 硬编码——说明我在持续迭代优化

  Q5：8 种 MQ 任务的差异化重试策略是怎么设计的？为什么不统一用一种？

  高分要点：
  - 每种任务的失败模式不同：简历解析可能因 AI 超时失败（需要较长重试延迟 45s），RAG 同步通常是瞬时网络问题（短延迟 20s
  即可）
  - QueueSpec 结构体定义了 per-type 的 retry_count（3-5 次）和 retry_delay（20-45 秒）
  - 三层 Exchange 拓扑：Topic（正常路由）→ Retry（延迟重投）→ DLX（死信兜底，人工介入）
  - 幂等 key 防止重复消费——即使消息被重投，业务侧也能识别并跳过
  - AsyncDispatchOption 模式：测试环境不依赖 RabbitMQ，直接同步执行

  Q6：Live2D 的 AI 指令生成是怎么保证实时性的？超时怎么办？

  高分要点：
  - AI 生成 Live2D 指令（表情、动作）是在每次 AI 回复时一起返回的结构化 JSON，不是独立调用
  - 结构化输出约束（runtime/structured_output.go）：通过 prompt engineering 约束 AI 输出固定格式的 JSON
  - 超时 fallback（docs/live2d-里程碑.md）：指令生成超时时使用默认表情/动作，不会阻塞主流程
  - 前端 live2dDirective.ts 解析指令后通过 Cubism 4 SDK 驱动，有平滑过渡动画
  - 热重载：AI Preset 改变后，Live2D Director 的 prompt 也随之更新（通过 RuntimeManager 指纹机制）

  Q7：从单体到微服务的迁移过程中，数据层是怎么处理的？是先拆库还是先拆服务？

  高分要点：
  - 采用的是先拆服务、共享数据库的策略——所有微服务仍然连接同一个 PostgreSQL 实例
  - 每个微服务有自己的 Repository 层和 Model 定义，但底层指向同一张表
  - 这是有意为之的渐进式方案：先验证服务边界和接口契约的正确性，再考虑数据库拆分
  - 好处：迁移风险低、可快速回滚；坏处：数据库仍然是单点
  - 未来方向：可以按服务做 schema 隔离，最终走向独立数据库

  Q8：这个项目的测试策略是怎样的？52 个测试文件覆盖了哪些场景？

  高分要点：
  - 单元测试：AI Runtime 各 Agent（builder、dynamic_client、interview_agent 等 15+ 个文件）、Service
  层业务逻辑（plan_service 的生成/调整/反馈各 1 个测试文件）
  - 集成测试：Repository 层（question_repo_test）、Bridge（http_test、ai_config_test）
  - 接口测试：通过 Postman Collection（MakeJob.postman_collection.json、MakeJob-RAG-API.postman_collection.json）
  - 前端测试：practiceRoute.test.ts、practiceCollectionMode.test.ts 等
  - AI 测试用 Mock Provider（ai/mock/）隔离外部依赖，验证业务逻辑而非 AI 效果
  - go test ./... 和 npm run test:web 均可独立运行 【待补充：具体覆盖率数据】

  ---