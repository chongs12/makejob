# MakeJob 仓库全局规则

本文件作用域：`D:\gogogo\makejob` 及其全部子目录。

## 注释规则

- 后续新增的每个函数，必须添加清晰、简洁、可直接说明职责的中文注释。
- 如果修改已有函数且该函数缺少注释，需一并补上中文注释。
- 注释重点说明函数用途、关键输入输出或必要约束，避免空话和重复代码字面含义。
- 注释应尽量贴近函数定义，保持统一风格，避免过长。

## 中文编码规则

- 任何包含中文的文件内容，在生成、读取、编辑、导出时，统一使用 `UTF-8` 编码。
- 读取含中文内容的文件时，必须显式按 `UTF-8` 处理，避免依赖系统默认编码。
- 新增文件或重写文件时，不得写入 `GBK`、`ANSI`、`Latin-1` 等非 `UTF-8` 编码内容。
- 如果发现仓库内中文乱码，默认先检查并统一到 `UTF-8` 编码后再继续修改。

## 前端复杂度治理决策规则

- 除非用户明确提出要做“前端复杂度治理 / 前端重构 / 大页面拆分 / 路由拆分 / 测试补齐 / 打包优化 / 工程整备”，否则不得主动执行这类工作。
- 在没有用户明确授权时，不得把前端复杂度治理列为默认下一步、阶段优先项或顺手附带修改项。
- 即使发现前端存在复杂度、耦合度、体积或可维护性风险，也只能作为背景说明简短记录，不得自动展开实施。
- 如果当前任务本身不是复杂度治理，但必须做极小范围的配套调整才能完成用户明确需求，应将改动控制在必要最小范围，不得借机扩展成重构。

## 指引文档进度同步规则

- 当某项开发工作明确以某个 `.md` 文档作为执行指引时，在该指引完成前，默认持续以该文档作为当前主指引，除非用户明确要求切换。
- 只要修改了与该指引直接相关的代码、接口、测试或页面，就必须在同一轮工作内同步更新对应指引文档的完成状态、进度说明或执行记录。
- 若本轮仅做分析、核查、验证或提出建议，但未修改与指引相关的代码，可不强制更新进度；一旦开始落代码，当步进度记录不得省略。
