# MakeJob 微服务重构 — 完整对比分析报告

> 生成日期: 2026-06-11 | 分析范围: 单体 vs 14个微服务+Gateway

---

# 一、总览

## 分析范围
- **单体基线**: `docs/backend/` 目录下的完整单体代码
- **微服务现状**: 14 个业务服务 + API Gateway
- **对比维度**: 字段名、类型、嵌套关系、用户状态、分页结构、Gateway 转换

## 统计摘要

| 服务 | 差异总数 | P0 | P1 | P2 |
|------|---------|----|----|-----|
| Question | 18 | 5 | 8 | 5 |
| Interview | 12 | 4 | 5 | 3 |
| Growth | 15 | 6 | 6 | 3 |
| Plan | 10 | 3 | 5 | 2 |
| Community | 9 | 3 | 4 | 2 |
| Membership | 4 | 2 | 1 | 1 |
| User/Auth | 3 | 0 | 2 | 1 |
| Gateway 转换层 | 8 | 3 | 3 | 2 |
| **合计** | **79** | **26** | **34** | **19** |

## 高风险服务 Top 3

1. **Growth** — 15 个差异，6 个 P0。`WeeklyStats`/`RecentStudyLogs`/`RecentInterviews`/`RecentPlans` 全部为空，成长页面几乎白屏。
2. **Question** — 18 个差异，5 个 P0。详情页缺 `is_favorited`/`user_note`/`tag_list`/`judge_summary`，答题反馈缺 `evaluation_mode`。
3. **Interview** — 12 个差异，4 个 P0。`FinishInterview` 返回极简（仅 id+status），报告页无法渲染；`InterviewStats` 缺 2 个字段。

## 分文件索引

| 文件 | 内容 |
|------|------|
| `对比分析_Question.md` | 题库服务 18 项差异 |
| `对比分析_Interview.md` | 面试服务 12 项差异 |
| `对比分析_Growth.md` | 成长服务 15 项差异 |
| `对比分析_Plan.md` | 计划服务 10 项差异 |
| `对比分析_Community.md` | 社区服务 9 项差异 |
| `对比分析_Membership_User.md` | 会员+用户服务 7 项差异 |
| `对比分析_Gateway.md` | Gateway 转换层 8 项差异 |
| `对比分析_附录.md` | 需人工确认清单 + 修复建议汇总 |

---

# 二、Question 服务


## 1. ListQuestions

**单体端点**: `GET /api/questions` → `PageResult{list: []Question}`
**微服务 RPC**: `QuestionService.ListQuestions` → `ListQuestionsResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `category_id` | ✅ 有 | ❌ 缺失 | P1 | 在 `QuestionSummary` proto 增加 `category_id` 字段 |
| `industry_id` | ✅ 有 | ❌ 缺失 | P1 | 在 `QuestionSummary` proto 增加 `industry_id` 字段 |
| `is_favorited` | ✅ 有 | ❌ 缺失 | P0 | 需要查询 `user_favorites` 表，按当前用户填充 |
| `content` | ✅ 有（列表也返回） | ❌ 缺失 | P2 | 列表不需要完整 content，可接受 |
| `tags` | ✅ 有 | ❌ 缺失 | P1 | 在 `QuestionSummary` 增加 `tags` 字段 |
| `is_active` | ✅ 有 | ❌ 缺失 | P2 | 列表默认只返回 active，可接受 |
| `created_at` | ✅ 有 | ❌ 缺失 | P2 | 在 `QuestionSummary` 增加 `created_at` |

---

## 2. GetQuestion（题目详情）

**单体端点**: `GET /api/questions/:id` → `QuestionDetail`
**微服务 RPC**: `QuestionService.GetQuestion` → `QuestionDetail`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `is_favorited` | ✅ 有 | ❌ 缺失（proto 有字段但未填充） | P0 | service 层查询 `user_favorites` 表填充 |
| `user_note` | ✅ `{id, title, content}` | ❌ 缺失 | P0 | service 层查询 `user_notes` 表填充 |
| `tag_list` | ✅ `[]string` | ❌ 缺失 | P1 | Gateway `normalizeQuestionDetailPayload` 已添加 `tag_list`，但仅限详情端点 |
| `industry_id` | ✅ 有 | ❌ 缺失 | P1 | proto 有 `industry_code` 但无 `industry_id` |
| `industry_name` | ✅ 有 | ❌ 缺失 | P1 | 需关联查询 `industries` 表 |
| `judge_summary` | ✅ `{evaluation_mode, test_cases}` | ❌ 缺失 | P1 | proto 中 `judge_config_json` 是字符串，前端需自行解析 |
| `solution` | ✅ 嵌套对象 `{approach, steps, complexity}` | ⚠️ `solution_json` 字符串 | P1 | Gateway `normalizeQuestionDetailPayload` 会解析 JSON 字符串为对象 |
| `answer_template` | ✅ 嵌套对象 | ⚠️ `answer_template_json` 字符串 | P1 | 同上，Gateway 已处理 |
| `options` | ✅ `[]string` 数组 | ⚠️ `options_json` 字符串 | P1 | Gateway `normalizeQuestionDetailPayload` 会解析 |

**说明**: `is_favorited` 和 `user_note` 是最严重的问题 — 题目详情页的核心交互状态完全缺失。

---

## 3. SubmitAnswer（提交答案）

**单体端点**: `POST /api/questions/:id/submit` → `SubmitAnswerResponse`
**微服务 RPC**: `QuestionService.SubmitAnswer` → `SubmitAnswerResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `ai_analysis` | ✅ AI 分析 JSON 字符串 | ❌ 缺失 | P1 | 需 AI Gateway 返回完整分析结果 |
| `evaluation_mode` | ✅ 有 | ❌ 缺失 | P0 | 前端根据此字段决定渲染方式（选择题/编程题/主观题） |
| `judge_summary` | ✅ `{all_passed, total_cases, passed_cases, results[]}` | ❌ 缺失 | P0 | 编程题答案提交后无法显示测试用例结果 |
| `score` | ❌ 单体无此字段 | ✅ 有（float64） | — | 新增字段，需前端适配 |
| `key_points` | ❌ 单体无此字段 | ✅ 有（[]string） | — | 新增字段，需前端适配 |
| `feedback` | ❌ 单体无此字段 | ✅ 有（string） | — | 新增字段，需前端适配 |

**说明**: 单体的 `judge_summary` 包含每个测试用例的 input/expected/actual/passed 详情，微服务完全缺失。

---

## 4. RunCode（运行代码）

**单体端点**: `POST /api/questions/:id/run` → `RunCodeResponse`
**微服务 RPC**: `QuestionService.RunCode` → `RunCodeResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `evaluation_mode` | ✅ 有 | ❌ 缺失 | P1 | proto 增加字段或从题目信息获取 |
| `judge_summary` | ✅ `{all_passed, total_cases, passed_cases, results[]}` | ❌ 缺失 | P0 | 前端无法显示逐个测试用例的通过/失败状态 |
| `passed` | ✅ 布尔值 | ⚠️ `success` 字段名不同 | P1 | 确认前端读取的是 `success` 还是 `passed` |

**说明**: `judge_summary.results[]` 包含每个测试用例的 input/expected/actual/passed，是代码题核心交互。

---

## 5. ToggleFavorite（收藏切换）

**单体端点**: `POST /api/questions/:id/favorite` → `{is_favorited: bool}`
**微服务 RPC**: `CreateFavorite` / `DeleteFavorite` → `Empty`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `is_favorited` | ✅ 返回切换后的状态 | ❌ 无返回值 | P0 | 两个 RPC 都返回 Empty，前端无法知道当前状态 |

**说明**: 单体是一个 toggle 端点返回新状态，微服务拆成 create/delete 两个端点且无返回值。

---

## 6. ListFavorites（收藏列表）

**单体端点**: `GET /api/user/favorites` → `PageResult{list: []UserFavorite}`
**微服务 RPC**: `QuestionService.ListFavorites` → `FavoriteListResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `category_name` | ✅ 有 | ❌ 缺失 | P1 | `ListFavorites` 的 `QuestionSummary` 未填充 `category_name` |
| `content` | ✅ 有 | ❌ 缺失 | P2 | 列表不需要完整 content |

---

## 7. GetPracticeStats（练习统计）

**单体端点**: `GET /api/user/practice-stats` → `UserPracticeStats`
**微服务 RPC**: `QuestionService.GetUserPracticeStats` → `UserPracticeStats`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `streak_days` | ✅ 有 | ❌ 缺失 | P1 | proto 有字段但 service 层未填充 |
| `today_count` | ✅ 有 | ❌ 缺失（始终为 0） | P1 | 需查询当天的 `user_question_records` |
| `category_stats[].category_id` | ✅ 有 | ❌ 缺失 | P2 | proto `CategoryStat` 无 `category_id` 字段 |

---

## 8. GetPracticeRecommendations（练习推荐）

**单体端点**: `GET /api/user/practice-recommendations` → `PracticeRecommendationResponse`
**微服务 RPC**: `QuestionService.GetPracticeRecommendations` → `PracticeRecommendationResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| 结构差异 | `items[]` 每项包含完整 `QuestionDetail` | `questions[]` 每项仅 `QuestionSummary` 级别 | P0 | 推荐页无法展示题目详情，需嵌入完整题目对象 |
| `items[].question.solution` | ✅ 嵌套对象 | ❌ 缺失 | P1 | 推荐的题目需要展示解题思路 |
| `items[].question.judge_config` | ✅ 嵌套对象 | ❌ 缺失 | P1 | 编程题需要 judge 配置 |
| `items[].occurrence_count` | ✅ 有 | ❌ 始终为 0 | P1 | 需从 LearningArchive 聚合 |
| `items[].primary_question_set` | ✅ 有 | ❌ 空字符串 | P2 | 需关联 question_sets |
| `items[].related_question_sets` | ✅ 有 | ❌ 空数组 | P2 | 同上 |
| `items[].recommended_actions` | ✅ 有 | ❌ 空数组 | P2 | 同上 |

---

## 9. GetWrongQuestions（错题列表）

**单体端点**: `GET /api/user/wrong-questions` → `PageResult{list: []WrongQuestion}`
**微服务 RPC**: `QuestionService.GetWrongQuestions` → `WrongQuestionListResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `last_answer` | ✅ 用户上次作答内容 | ❌ 始终为空字符串（SQL 硬编码 `''`） | P1 | 修改 SQL 查询，JOIN `user_question_records` 获取实际答案 |

---

## 10. ExamResponse / SubmitExamResponse

**单体端点**: `POST /api/exams/submit` → `ExamResult`
**微服务 RPC**: `QuestionService.SubmitExam` → `SubmitExamResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `details[].user_answer` | ✅ 用户作答 | ❌ 缺失 | P1 | proto `QuestionResult` 无此字段 |
| `details[].correct_answer` | ✅ 正确答案 | ❌ 缺失（biz 有但未映射） | P0 | service 层需映射 `CorrectAnswer` 到 proto |
| `details[].explanation` | ✅ 解析 | ❌ 缺失 | P1 | proto `QuestionResult` 无此字段，仅有 `feedback` |

---

## 11. GetRandomExam / GenerateTimedExam

**单体端点**: `POST /api/exams/random` → `ExamResponse`
**微服务 RPC**: `QuestionService.GetRandomExam` → `ExamResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `exam_id` | ✅ string 类型 | ⚠️ `uint64` 类型 | P2 | 确认前端类型处理 |
| `time_limit` | ✅ 字段名 `time_limit` | ⚠️ 字段名 `time_limit_minutes` | P1 | 确认前端读取的字段名 |
| `questions[].options` | ✅ 数组 | ❌ 缺失（RandomSelect 不加载） | P0 | `RandomSelect` 方法需加载 `OptionsJSON` |
| `questions[].answer` | ✅ 有 | ❌ 缺失 | P1 | 考试模式可能不需要显示答案 |
| `questions[].tags` | ✅ 有 | ❌ 缺失 | P2 | 题目列表不需要 tags |

---

## 12. ListQuestionSets（题集列表）

**单体端点**: `GET /api/question-sets` → `[]QuestionSetSummary`
**微服务 RPC**: `QuestionService.ListQuestionSets` → `ListQuestionSetsResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `difficulty` | ✅ 有 | ❌ 缺失 | P2 | proto `QuestionSetSummary` 无此字段 |
| `cover_image` | ✅ 有 | ❌ 缺失 | P2 | proto 无此字段 |
| 返回结构 | 直接返回数组 | `{items: [], page_result: {}}` | P1 | Gateway `normalizeQuestionSetsPayload` 处理了 `focus_tags` 和 `questions` 数组 |

---

## 13. CategoryNode（分类树）

**单体端点**: `GET /api/categories` → `[]CategoryTree`
**微服务 RPC**: `QuestionService.ListCategories` → `CategoryTreeResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `question_count` | ✅ 每个分类的题目数 | ❌ 缺失 | P1 | 需要聚合查询或在 category 表中维护计数 |
| `icon` | ✅ 有 | ❌ 缺失 | P2 | proto `CategoryNode` 无此字段 |
| `description` | ✅ 有 | ❌ 缺失 | P2 | proto 无此字段 |

---

# 三、Interview 服务


## 1. CreateInterview

**单体端点**: `POST /api/interviews` → `InterviewResponse`
**微服务 RPC**: `InterviewService.CreateInterview` → `InterviewResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `first_question.live2d_directive` | ✅ `{emotion, action, source, ...}` | ❌ 缺失（proto 有字段但 `toProtoQuestion` 未映射） | P0 | 修改 `toProtoQuestion` 函数映射 `Live2DDirective` |
| `async_task_id` | ✅ 有 | ❌ 始终为 0 | P2 | 异步任务 ID 未填充 |
| `task_status` | ✅ 有 | ❌ 始终为空 | P2 | 同上 |
| `task_error` | ✅ 有 | ❌ 始终为空 | P2 | 同上 |

**说明**: `live2d_directive` 缺失导致 Live2D 虚拟形象无法做出正确表情和动作。

---

## 2. GetInterview（面试详情）

**单体端点**: `GET /api/interviews/:id` → `InterviewDetailResponse`
**微服务 RPC**: `InterviewService.GetInterview` → `InterviewDetail`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `live2d_model_key` | ✅ 有 | ❌ 缺失 | P1 | proto `InterviewDetail` 无此字段 |
| `report`（嵌套） | ✅ 有（详情页内嵌报告） | ❌ 始终为 nil（需单独调用 `GetReport`） | P1 | 需前端改为两步调用，或在 `GetInterview` 中填充摘要 |
| `score` 类型 | ✅ float64 | ⚠️ int32（截断小数） | P2 | proto 定义为 `int32`，丢失精度 |

---

## 3. ListInterviews（面试列表）

**单体端点**: `GET /api/interviews` → `PageResult{list: []MockInterview}`
**微服务 RPC**: `InterviewService.ListInterviews` → `ListInterviewsResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `industry_code` | ✅ 有 | ❌ 缺失 | P1 | 列表页需要显示行业信息 |
| `first_question` | ✅ 有 | ❌ 缺失 | P2 | 列表不需要完整题目 |
| `interview_mode` | ✅ 有 | ❌ 缺失 | P1 | 列表页需要区分标准/实时/编码模式 |

---

## 4. SubmitAnswer（提交面试答案）

**单体端点**: `POST /api/interviews/:id/answer` → `InterviewAnswerResponse`
**微服务 RPC**: `InterviewService.SubmitAnswer` → `AnswerFeedback`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| 结构差异 | `{feedback: {...}, next_question, is_finished}` | 扁平 `{score, is_correct, feedback, key_points, suggestions, follow_up, next_question}` | P0 | Gateway `normalizeInterviewAnswerPayload` 已重构为单体格式，但 `is_finished` 始终为 false |
| `is_finished` | ✅ 标记面试是否结束 | ❌ 缺失（Gateway 设为 false） | P0 | 需在 service 层计算 `is_finished` 状态 |

**说明**: Gateway 的 `normalizeInterviewAnswerPayload` 会将扁平结构重组为嵌套结构，但 `is_finished` 字段被硬编码为 `false`。

---

## 5. FinishInterview（结束面试）

**单体端点**: `POST /api/interviews/:id/finish` → `InterviewReportResponse`
**微服务 RPC**: `InterviewService.FinishInterview` → `InterviewReport`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `report` | ✅ 完整报告对象 | ❌ 缺失（仅返回 `interview_id` + `status`） | P0 | 异步生成报告，前端需轮询 `GetReport` |
| `duration_seconds` | ✅ 有 | ❌ 缺失 | P1 | 需计算 `finished_at - created_at` |
| `completed_at` | ✅ 有 | ❌ 缺失 | P1 | 需返回 `finished_at` 时间戳 |
| `async_task_id` | ✅ 有 | ❌ 缺失 | P2 | 异步任务追踪 |

**说明**: 这是 P0 问题 — 前端调用 `finish` 后期望立即拿到报告数据，但微服务只返回一个 "generating" 状态。Gateway 的 `normalizeInterviewReportPayload` 会重组结构，但数据本身就是空的。

---

## 6. GetReport（获取报告）

**单体端点**: `GET /api/interviews/:id/report` → `InterviewReportResponse`
**微服务 RPC**: `InterviewService.GetReport` → `InterviewReport`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `duration_seconds` | ✅ 有 | ❌ 始终为 0 | P1 | 需在 report 生成时计算并存储 |
| `completed_at` | ✅ 有 | ❌ 始终为空 | P1 | 需返回 `interview.finished_at` |
| `async_task_id` | ✅ 有 | ❌ 始终为 0 | P2 | 异步任务追踪 |

---

## 7. GetInterviewStats（面试统计）

**单体端点**: 无直接对应（Growth 服务内部调用）
**微服务 RPC**: `InterviewService.GetInterviewStats` → `InterviewStats`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `total_questions_answered` | ✅ 有 | ❌ 始终为 0（proto 有字段但 SQL 未查询） | P1 | 需增加 SQL 聚合查询 |
| `avg_accuracy` | ✅ 有 | ❌ 始终为 0（proto 有字段但 SQL 未查询） | P1 | 需增加 SQL 聚合查询 |

**说明**: 这两个字段影响 Growth 服务的 `GrowthSummary` 聚合。

---

## 8. InterviewMessage（消息结构）

**单体**: 消息包含完整的 `question` 子对象（含 `live2d_directive`）
**微服务**: `InterviewMessage.question` 子对象不含 `live2d_directive`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `question.live2d_directive` | ✅ 有 | ❌ 缺失 | P0 | 修改 `toProtoQuestion` 映射 |
| `content` 处理 | 原始 JSON 内容 | ⚠️ 经过 `NormalizeMessageContent` 只返回 question 文本 | P1 | 确认前端是否需要原始 JSON |

---

## 9. CodingDiagnosis（编程诊断）

**单体**: `coding_diagnostics[].evidence` 是字符串数组
**微服务**: `evidence` 由 `splitEvidenceSummary` 从 `evidence_summary` 拆分生成

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `process_summary` | ✅ 独立字段 | ⚠️ 与 `evidence_summary` 相同值 | P2 | 语义相同但来源不同 |
| `evidence` | ✅ 原始证据数组 | ⚠️ 从 `evidence_summary` 拆分 | P2 | 拆分逻辑可能不准确 |

---

# 四、Growth 服务


## 1. GetGrowthSummary（成长总览）

**单体端点**: `GET /api/user/growth-summary` → `GrowthSummaryResponse`
**微服务 RPC**: `GrowthService.GetGrowthSummary` → `GrowthSummary`

这是差异最集中的接口，影响成长主页的核心展示。

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `weekly_stats` | ✅ 周统计数据 `[{week, questions_answered, interviews_taken, avg_score}]` | ❌ 始终为空数组 `[]` | P0 | 需要从 study_log 和 interview 表按周聚合 |
| `recent_study_logs` | ✅ 最近学习记录 `[{id, date_key, summary, focus_task_title, completed_count, ...}]` | ❌ 始终为空数组 `[]` | P0 | 需查询 `study_logs` 表最近 N 条 |
| `recent_interviews` | ✅ 最近面试 `[{id, status, score, total_questions, created_at, ended_at}]` | ❌ 始终为空数组 `[]` | P0 | 需调用 Interview 服务 `ListInterviews` |
| `recent_plans` | ✅ 最近计划 `[{id, title, status, total_tasks, completed_tasks, progress, start_date, end_date}]` | ❌ 始终为空数组 `[]` | P0 | 需调用 Plan 服务 `ListPlans` |
| `practice_stats.today_count` | ✅ 今日练习数 | ❌ 始终为 0 | P1 | 需查询当天 `user_question_records` |
| `practice_stats.category_stats` | ✅ 分类统计 `[{category_id, category_name, total, correct, accuracy_rate}]` | ❌ 始终为空数组 `[]` | P0 | 需从 Question 服务 `GetUserPracticeStats` 获取 |
| `completed_interview_count` | ✅ 已完成面试数 | ⚠️ 等于 `total_interviews`（未区分完成/总数） | P1 | 需 SQL 区分 `status='completed'` |
| `current_plan.id` | ✅ 计划 ID | ❌ 始终为 0 | P1 | Plan 服务返回了 ID 但 Growth 未映射 |
| `current_plan.status` | ✅ 计划状态 | ❌ 始终为空 | P1 | 同上 |
| `current_plan.next_task_title` | ✅ 下一个任务标题 | ❌ 始终为空 | P1 | 需查询 Plan 服务获取下一个 pending 任务 |
| `current_plan.next_task_source` | ✅ 任务来源 | ❌ 始终为空 | P2 | 需从任务元数据获取 |
| `current_plan.next_task_reason` | ✅ 推荐原因 | ❌ 始终为空 | P2 | 同上 |
| `focus_signals[].topic_code` | ✅ 有 | ❌ 始终为空 | P2 | LearningArchive 未返回此字段 |
| `focus_signals[].topic_problem_pattern` | ✅ 有 | ❌ 始终为空 | P2 | 同上 |
| `focus_signals[].related_question_sets` | ✅ 有 | ❌ 始终为空数组 | P2 | 同上 |

**说明**: 成长主页 6 个核心区块中有 4 个（周统计、最近学习、最近面试、最近计划）完全为空，页面大面积白屏。

---

## 2. GetWeeklyFocus（每周聚焦）

**单体端点**: `GET /api/user/weekly-focus` → `WeeklyFocusResponse`
**微服务 RPC**: `GrowthService.GetWeeklyFocus` → `WeeklyFocus`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `themes[].topic_codes` | ✅ 有 | ❌ 始终为空数组 | P1 | 需从 LearningArchive 获取关联 topic codes |
| `themes[].related_question_sets` | ✅ 有 | ❌ 始终为空数组 | P1 | 需从 Question 服务获取关联题集 |
| `themes[].suggestions` | ✅ 有 | ❌ 始终为空数组 | P1 | 需 AI 生成或从 FocusSignal 提取 |
| `themes[].interview_occurrence_count` | ✅ 有 | ❌ 始终为 0 | P1 | 需从 Interview 统计中获取 |
| `themes[].dominant_archive_phase` | ✅ 有 | ❌ 始终为空 | P2 | 需从 LearningArchive 聚合 |
| `themes[].dominant_archive_phase_label` | ✅ 有 | ❌ 始终为空 | P2 | 同上 |

---

## 3. SyncStudyLog（同步学习日志）

**单体端点**: `PUT /api/user/study-logs/daily` → `StudyLogResponse`
**微服务 RPC**: `GrowthService.SyncStudyLog` → `StudyLog`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| 结构完全不同 | `{id, date_key, summary, focus_task_title, completed_count, skipped_count, completed_titles, skipped_titles, latest_action_text, updated_at}` | `{id, user_id, action, ref_id, created_at}` | P0 | Proto 定义与单体完全不同，需重新设计 `StudyLog` message |
| `date_key` | ✅ 日期键 | ❌ 缺失 | P0 | 前端需要此字段展示日历 |
| `summary` | ✅ 学习摘要 | ❌ 缺失 | P1 | 需 AI 生成或从任务数据聚合 |
| `completed_count` | ✅ 完成任务数 | ❌ 缺失 | P1 | 需从 Plan 服务获取 |
| `skipped_count` | ✅ 跳过任务数 | ❌ 缺失 | P1 | 同上 |
| `completed_titles` | ✅ 完成任务标题列表 | ❌ 缺失 | P1 | 同上 |
| `latest_action_text` | ✅ 最近操作文本 | ❌ 缺失 | P2 | 需从学习记录聚合 |

**说明**: 这是结构性差异 — 单体的 `StudyLogResponse` 是一个丰富的日聚合对象，微服务的 `StudyLog` 只是一个原始事件记录。

---

# 五、Plan 服务


## 1. GetPlan / GetCurrentPlan（计划详情）

**单体端点**: `GET /api/plans/:id` / `GET /api/plans/current` → `PlanDetailResponse`
**微服务 RPC**: `PlanService.GetPlan` / `GetCurrentPlan` → `PlanDetail`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `industry_id` | ✅ 数字 ID | ❌ 缺失 | P1 | proto 无此字段，仅有 `industry`（字符串） |
| `phase` | ✅ 当前阶段 `foundation/drill/mock` | ❌ 缺失 | P0 | 前端需要展示当前学习阶段 |
| `phase_goal` | ✅ 阶段目标 | ❌ 缺失 | P0 | 前端需要展示阶段说明 |
| `entry_phase` | ✅ 入口阶段 | ❌ 缺失 | P2 | 用于计划调整 |
| `adjustment_summaries` | ✅ 调整摘要列表 | ❌ 缺失 | P1 | 需从调整记录表查询 |
| `adjustment_reason_codes` | ✅ 调整原因代码 | ❌ 缺失 | P2 | 同上 |
| `phase_blueprint_summary` | ✅ 阶段蓝图 `[{phase, phase_goal, start_day, end_day, expected_task_types, exit_criteria}]` | ❌ 缺失 | P0 | 前端需要展示学习路线图 |
| `tasks[].source` | ✅ 任务来源 `weekly_focus/weak_topic/goal/...` | ❌ 缺失 | P1 | 需在任务创建时记录来源 |
| `tasks[].source_label` | ✅ 来源显示文本 | ❌ 缺失 | P1 | 同上 |
| `tasks[].reason` | ✅ 推荐原因 | ❌ 缺失 | P1 | 同上 |
| `tasks[].priority_explanation` | ✅ 优先级说明 | ❌ 缺失 | P2 | 同上 |
| `tasks[].source_ref` | ✅ 来源引用 | ❌ 缺失 | P2 | 同上 |
| `tasks[].collection_hint` | ✅ 集合提示 | ❌ 缺失 | P2 | 同上 |
| `tasks[].due_date` | ✅ 截止日期 | ❌ 缺失 | P1 | proto `TaskDetail` 无此字段 |
| `tasks[].phase_goal` | ✅ 阶段目标 | ❌ 缺失 | P1 | proto `TaskDetail` 无此字段 |

---

## 2. ListPlans（计划列表）

**单体端点**: `GET /api/plans` → `PageResult{list: []LearningPlan}`
**微服务 RPC**: `PlanService.ListPlans` → `ListPlansResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `description` | ✅ 有 | ❌ 缺失（`PlanSummary` 无此字段） | P2 | 列表不需要完整描述 |
| `level` | ✅ 有 | ❌ 缺失 | P2 | 列表可不展示 |
| `duration_days` | ✅ 有 | ❌ 缺失 | P1 | 列表页需要展示计划天数 |
| `daily_study_minutes` | ✅ 有 | ❌ 缺失 | P2 | 列表可不展示 |

---

## 3. CreatePlan（创建计划）

**单体端点**: `POST /api/plans` → `PlanDetailResponse`
**微服务 RPC**: `PlanService.CreatePlan` → `PlanResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| 返回内容 | 完整 `PlanDetailResponse` | 仅 `{plan_id, status, created_at}` | P1 | 异步生成，前端需轮询 `GetPlan` 获取完整数据 |
| `tasks` | ✅ 立即返回任务列表 | ❌ 缺失（异步生成中） | P1 | 前端需等待生成完成后重新获取 |

---

## 4. UpdateTaskStatus（更新任务状态）

**单体端点**: `PUT /api/plans/:id/tasks/:taskId` → `null`
**微服务 RPC**: `PlanService.UpdateTaskStatus` → `UpdateTaskStatusResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| 返回值 | `null`（无返回） | `{task_status, plan_status, completed_tasks, total_tasks, progress}` | — | 微服务返回更丰富，需前端适配 |

---

## 5. SubmitTaskFeedback（提交任务反馈）

**单体端点**: `POST /api/plans/:id/tasks/:taskId/feedback` → `null`
**微服务 RPC**: `PlanService.SubmitTaskFeedback` → `FeedbackResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| 返回值 | `null` | `{feedback_id, status, diagnosis, suggestions}` | — | 微服务返回更丰富，需前端适配 |
| `diagnosis` | — | ⚠️ 始终为空字符串（异步生成） | P1 | 前端需轮询或等待 MQ 回调 |

---

## 6. AdjustPlan（调整计划）

**单体端点**: `POST /api/plans/:id/adjust` → `PlanDetailResponse`（调整后的完整计划）
**微服务 RPC**: `PlanService.AdjustPlan` → `AdjustPlanResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| 返回结构 | 完整 `PlanDetailResponse` | `{tasks_added, tasks_removed, tasks_reordered, adjustment_summary, updated_tasks[]}` | P1 | 前端需要适配新结构，或 Gateway 重组 |

---

## 7. GetProgress（进度统计）

**单体端点**: `GET /api/plans/:id/progress` → `PlanProgressResponse`
**微服务 RPC**: `PlanService.GetProgress` → `PlanProgressResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| 基本对齐 | ✅ | ✅ | — | 结构基本一致 |

此接口差异较小，基本对齐。

---

# 六、Community 服务


## 1. ListPosts（帖子列表）

**单体端点**: `GET /api/community/posts` → `PageResult{list: []CommunityPostItem}`
**微服务 RPC**: `CommunityService.ListPosts` → `ListPostsResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `is_pinned` | ✅ 是否置顶 | ❌ 缺失 | P1 | 需在帖子表增加 `is_pinned` 字段 |
| `is_recommended` | ✅ 是否推荐 | ❌ 缺失 | P1 | 需在帖子表增加 `is_recommended` 字段 |
| `author.role` | ✅ 作者角色 | ❌ 缺失（`PostSummary` 无 `author_role`） | P1 | 需关联 user 表获取角色 |
| `author` 结构 | ✅ 嵌套对象 `{id, username, avatar, role}` | ⚠️ 扁平字段 `author_id, author_name, author_avatar` | P1 | Gateway `normalizeCommunityPostListPayload` 已构造嵌套 `author` 对象 |
| `tags` 格式 | ✅ `[]string` 数组 | ⚠️ CSV 字符串 `"tag1,tag2"` | P1 | Gateway `normalizeCommunityPostListPayload` 已拆分为数组 |
| `is_liked` | ✅ 当前用户是否点赞 | ❌ 缺失（proto 有字段但未填充） | P0 | 需查询 `community_interactions` 表 |
| `is_author` | ✅ 当前用户是否为作者 | ❌ 缺失（proto 有字段但未填充） | P0 | 需比较 `author_id` 与当前用户 ID |

**说明**: Gateway 的 normalizer 已处理了 `tags` 拆分和 `author` 嵌套，但 `is_liked` 和 `is_author` 需要在 service 层计算。

---

## 2. GetPost（帖子详情）

**单体端点**: `GET /api/community/posts/:id` → `CommunityPostItem`
**微服务 RPC**: `CommunityService.GetPost` → `PostDetail`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `is_pinned` | ✅ 有 | ❌ 缺失 | P1 | 同列表 |
| `is_recommended` | ✅ 有 | ❌ 缺失 | P1 | 同列表 |
| `author.role` | ✅ 有 | ❌ 缺失 | P1 | 需关联 user 表 |
| `is_liked` | ✅ 有 | ❌ 缺失（proto 有但未填充） | P0 | 需查询 `community_interactions` |
| `is_author` | ✅ 有 | ❌ 缺失（proto 有但未填充） | P0 | 需比较 author_id 与当前用户 |

---

## 3. ListComments（评论列表）

**单体端点**: `GET /api/community/posts/:id/comments` → `[]CommunityCommentItem`
**微服务 RPC**: `CommunityService.ListComments` → `ListCommentsResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `author.role` | ✅ 有 | ❌ 缺失 | P1 | proto `Comment` 无 `author_role` |
| `author` 结构 | ✅ 嵌套对象 | ⚠️ 扁平 `author_id, author_name, author_avatar` | P1 | Gateway `normalizeCommunityCommentListPayload` 已构造嵌套对象 |
| `is_author` | ✅ 有 | ❌ 缺失（proto 有但未填充） | P0 | 需比较 author_id 与当前用户 |

---

## 4. CreatePost（创建帖子）

**单体端点**: `POST /api/community/posts` → `CommunityPostItem`（完整帖子）
**微服务 RPC**: `CommunityService.CreatePost` → `Post`（仅 id, title, created_at）

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| 返回内容 | 完整帖子对象 | 仅 `{id, title, created_at}` | P1 | 前端创建后可能需要立即展示完整帖子 |

---

## 5. UpdatePost（更新帖子）

**单体端点**: `PUT /api/community/posts/:id` → `CommunityPostItem`
**微服务 RPC**: `CommunityService.UpdatePost` → `Post`（仅 id, title, created_at）

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| 返回内容 | 完整帖子对象 | 仅 `{id, title, created_at}` | P1 | 同 CreatePost |

---

## 6. DeletePost（删除帖子）

**单体端点**: `DELETE /api/community/posts/:id` → `{id: uint}`
**微服务 RPC**: `CommunityService.DeletePost` → `Empty`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| 返回值 | `{id}` | 空 | P2 | 前端可用于确认删除哪个帖子 |

---

## 7. CreateComment（创建评论）

**单体端点**: `POST /api/community/posts/:id/comments` → `CommunityCommentItem`
**微服务 RPC**: `CommunityService.CreateComment` → `Comment`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `author` 结构 | ✅ 嵌套对象 | ⚠️ 扁平字段 | P1 | 需前端自行构造或 Gateway 处理 |
| `author_id` 来源 | 从 auth context | ⚠️ 从 request body（安全问题） | P1 | 需改为从 auth context 获取 |

---

# 七、Membership + User 服务


## Membership 服务

### 1. GetMembershipStatus（会员状态）

**单体端点**: `GET /api/membership/status` → `MembershipStatusResponse`
**微服务 RPC**: `MembershipService.GetMembershipStatus` → `MembershipStatus`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `daily_practice_limit` | ✅ 每日练习限制 | ❌ 缺失 | P0 | 前端需要限制用户操作次数 |
| `daily_interview_limit` | ✅ 每日面试限制 | ❌ 缺失 | P0 | 同上 |
| `practice_used_today` | ✅ 今日已练习数 | ❌ 缺失 | P1 | 需查询当天操作记录 |
| `interview_used_today` | ✅ 今日已面试数 | ❌ 缺失 | P1 | 同上 |
| `expires_at` | ✅ 字段名 | ⚠️ proto 字段名 `expire_at`（无 s） | P2 | 确认前端读取的字段名 |

**说明**: `daily_practice_limit` 和 `daily_interview_limit` 是会员功能限制的核心字段，缺失导致前端无法展示剩余次数。

---

### 2. ListPlans（会员计划列表）

**单体端点**: `GET /api/membership/plans` → `[]MembershipPlanResponse`
**微服务 RPC**: `MembershipService.ListPlans` → `ListPlansResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `original_price` | ✅ 原价 | ❌ 缺失 | P1 | 前端需要展示折扣信息 |
| `is_popular` | ✅ 是否热门推荐 | ❌ 缺失 | P1 | 前端需要高亮推荐套餐 |

---

### 3. ListOrders（订单列表）

**单体端点**: `GET /api/membership/orders` → `PageResult{list: []OrderResponse}`
**微服务 RPC**: `MembershipService.ListOrders` → `ListOrdersResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| 分页结构 | `{list, total, page, page_size}` | `{orders, total}` | P1 | 缺少 `page` 和 `page_size` 字段 |

---

## User 服务

### 4. GetProfile / Login / Register

**单体端点**: `GET /api/auth/me` / `POST /api/auth/login` → `UserProfile` / `LoginResponse`
**微服务 RPC**: `UserService.GetProfile` / `Login` → `UserProfile` / `AuthResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `membership_level` | ✅ 在 UserProfile 中 | ✅ 有 | — | 对齐 |
| `membership_expire_at` | ❌ 单体 UserProfile 无此字段 | ✅ proto 有 | — | 微服务更丰富 |
| `user` 嵌套在 AuthResponse | ✅ `{token, refresh_token, expires_at, user: {...}}` | ✅ 同结构 | — | 对齐 |

基本对齐，无重大差异。

---

### 5. AdminListUsers（管理员用户列表）

**单体端点**: `GET /api/admin/users` → `PageResult{list: []AdminUserListItem}`
**微服务 RPC**: `UserService.AdminListUsers` → `AdminListUsersResponse`

| 项目 | 单体返回 | 微服务返回 | 优先级 | 修复建议 |
|------|---------|-----------|--------|---------|
| `updated_at` | ✅ 有 | ❌ 缺失 | P2 | proto `AdminUserInfo` 无此字段 |

差异较小。

---

# 八、Gateway 转换层


## 概述
Gateway 是 REST→gRPC 的转换层，所有前端请求经过 Gateway 的 `normalizeJSONValue()` + `adaptLegacyResponseByPath()` 处理。以下分析 Gateway 转换可能导致的字段丢失或结构变化。

---

## 1. unwrapSingleListField — 数组外层键被丢弃

**影响**: 所有返回单一数组字段的 gRPC 响应

| 单体返回 | Gateway 转换后 | 影响端点 |
|---------|---------------|---------|
| `{industries: [...]}` | `[...]` | `ListIndustries` |
| `{categories: [...]}` | `[...]` | `ListCategories` |
| `{templates: [...]}` | `[...]` | `ListPromptTemplates` |
| `{models: [...]}` | `[...]` | `ListLive2DModels` |
| `{sources: [...]}` | `[...]` | `GetScraperSources` |

**优先级**: P1
**修复建议**: 确认前端是否直接读取数组还是读取包装键。如果前端读取 `response.data.industries`，则会得到 `undefined`。

---

## 2. flattenPageResult — 分页数组键被替换为 `list`

**影响**: 所有分页列表端点

| 单体返回 | Gateway 转换后 | 影响端点 |
|---------|---------------|---------|
| `{questions: [...], page_result: {total, page, page_size}}` | `{list: [...], total: 10, page: 1, page_size: 20}` | `ListQuestions`, `ListFavorites`, `AdminListQuestions` |
| `{interviews: [...], page_result: {...}}` | `{list: [...], total, page, page_size}` | `ListInterviews` |
| `{posts: [...], page_result: {...}}` | `{list: [...], total, page, page_size}` | `ListPosts`, `ListMyPosts` |
| `{users: [...], page_result: {...}}` | `{list: [...], total, page, page_size}` | `ListUsers` |
| `{logs: [...], page_result: {...}}` | `{list: [...], total, page, page_size}` | `ListAICallLogs` |

**优先级**: P0
**修复建议**: 前端所有分页列表读取路径从 `response.data.questions` 改为 `response.data.list`，或在 Gateway 中保留原始键名。

---

## 3. normalizeInterviewReportPayload — 报告结构重组

**影响**: `FinishInterview` 和 `GetReport` 的 `/report` 和 `/finish` 端点

**单体返回**:
```json
{
  "interview_id": 1,
  "status": "completed",
  "report": { "overall_score": 85, "total_questions": 10, ... },
  "duration_seconds": 1800,
  "completed_at": "..."
}
```

**微服务 gRPC 返回** (扁平):
```json
{
  "interview_id": 1,
  "status": "completed",
  "overall_score": 85,
  "total_questions": 10,
  ...
}
```

**Gateway 转换后**:
```json
{
  "interview_id": 1,
  "status": "completed",
  "report": { "interview_id": 1, "status": "completed", "overall_score": 85, ... },
  "duration_seconds": 0,
  "completed_at": ""
}
```

**优先级**: P0
**问题**: Gateway 会将扁平结构重组为嵌套结构，但 `duration_seconds` 和 `completed_at` 始终为零值/空值（微服务未填充），导致 `report` 内部也包含重复的顶层字段。

---

## 4. normalizeInterviewAnswerPayload — 答案反馈重组

**影响**: `SubmitAnswer` 的 `/answer` 端点

**单体返回**:
```json
{
  "feedback": { "score": 85, "is_correct": true, "feedback": "...", "key_points": [...] },
  "next_question": { "question": "...", ... },
  "is_finished": false
}
```

**微服务 gRPC 返回** (扁平):
```json
{
  "score": 85, "is_correct": true, "feedback": "...", "key_points": [...],
  "suggestions": "...", "follow_up": "...", "next_question": { ... }
}
```

**Gateway 转换后**:
```json
{
  "feedback": { "score": 85, "is_correct": true, "feedback": "...", ... },
  "next_question": { ... },
  "is_finished": false
}
```

**优先级**: P0
**问题**: `is_finished` 被硬编码为 `false`，前端无法知道面试是否结束。

---

## 5. normalizePracticeRecommendationPayload — 推荐结构重组

**影响**: `GetPracticeRecommendations` 的 `/recommendations` 端点

**单体返回**:
```json
{
  "focus_tags": ["tag1"],
  "items": [
    {
      "question": { "id": 1, "title": "...", "content": "...", ... },
      "focus_tag": "tag1",
      "topic_code": "...",
      ...
    }
  ]
}
```

**微服务 gRPC 返回**:
```json
{
  "questions": [
    { "question_id": 1, "title": "...", "difficulty": "easy", "type": "choice", ... }
  ],
  "reason": "...",
  "focus_tags": ["tag1"]
}
```

**Gateway 转换后**: Gateway 会尝试重组，但 `question` 对象只是 `QuestionSummary` 级别，不是完整的 `QuestionDetail`。

**优先级**: P0
**问题**: 推荐的题目缺少 `content`、`options`、`answer`、`explanation` 等详情字段，前端无法展示完整题目。

---

## 6. normalizeQuestionSetsPayload — 题集数组保证

**影响**: `ListQuestionSets` 和 `GetQuestionSetDetail`

Gateway 确保 `focus_tags` 和 `questions` 始终为数组（即使后端返回 null）。

**优先级**: P2 — 已正确处理

---

## 7. normalizeGrowthSummaryPayload — 成长数据数组保证

**影响**: `GetGrowthSummary`

Gateway 确保以下 4 个数组字段始终存在（即使后端返回空）:
- `recent_study_logs`
- `recent_interviews`
- `recent_plans`
- `weekly_stats`

**优先级**: P2 — Gateway 已补偿，但数据始终为空

---

## 8. omitempty 零值字段丢失

**影响**: 所有没有专用 normalizer 的端点

protobuf 生成的 Go 结构体使用 `json:"...,omitempty"`，导致:
- 空字符串字段被省略
- 零值整数字段被省略
- `false` 布尔字段被省略
- 空数组字段被省略
- nil 嵌套消息被省略

**受影响的端点** (无专用 normalizer):
- `GetQuestion` — `is_favorited: false` 被省略
- `GetPost` — `is_liked: false`, `is_author: false` 被省略
- `ListComments` — `is_author: false` 被省略
- `GetMembershipStatus` — `is_active: false` 被省略

**优先级**: P1
**修复建议**: 为这些端点添加 Gateway normalizer，使用 `ensureBoolField()` 补偿零值布尔字段。

---

# 九、附录


## 一、需前后端对齐确认清单

以下接口因单体代码不完整或微服务实现未最终确定，无法完全定论，需人工查验：

### 1. Question 服务

| 接口 | 待确认项 |
|------|---------|
| `GetQuestion.is_favorited` | proto 定义了 `is_favorited` 字段（field 15），但 service 层是否已实现查询逻辑？需检查 `app/question/internal/service/question.go` |
| `GetQuestion.user_note` | proto 未定义 `user_note` 嵌套消息。是否需要新增 proto 字段？还是前端改为单独调用 `ListNotes`？ |
| `SubmitAnswer.judge_summary` | 单体的 `judge_summary` 包含每个测试用例详情。微服务是否需要在 `SubmitAnswerResponse` 中增加此字段？还是前端通过 `RunCode` 获取？ |
| `ListQuestions` 返回内容 | 单体列表返回完整 `Question` 对象。微服务只返回 `QuestionSummary`。确认前端列表页是否真的需要 `content`、`options` 等字段 |
| `GetPracticeRecommendations` 结构 | 单体 `items[].question` 是完整 `QuestionDetail`。微服务 `questions[]` 只有摘要。确认推荐页是否需要完整题目 |

### 2. Interview 服务

| 接口 | 待确认项 |
|------|---------|
| `FinishInterview` 返回值 | 单体返回完整报告。微服务返回 `generating` 状态。确认前端是否有轮询 `GetReport` 的逻辑？ |
| `InterviewDetail.report` | proto 定义了 `report` 子消息（field 7），但 service 从未填充。是否需要在 `GetInterview` 中嵌入报告摘要？ |
| `InterviewMessage.content` | `NormalizeMessageContent` 将 assistant 消息的 JSON 内容提取为纯文本。确认前端是否需要原始 JSON（包含 `language`、`starter_code` 等）？ |
| `InterviewStats` 缺失字段 | `total_questions_answered` 和 `avg_accuracy` 在 proto 中定义但 SQL 未查询。确认 Growth 服务是否依赖这两个字段？ |

### 3. Growth 服务

| 接口 | 待确认项 |
|------|---------|
| `GetGrowthSummary.weekly_stats` | 始终为空。确认前端是否有降级显示逻辑？还是直接白屏？ |
| `GetGrowthSummary.recent_*` | 三个 `recent_*` 数组始终为空。确认 Growth 页面是否有空状态设计？ |
| `SyncStudyLog` 结构差异 | 单体返回丰富的日聚合对象，微服务返回原始事件。确认哪个是正确的设计？ |
| `CompletedInterviewCount` | 始终等于 `TotalInterviews`。确认是否有 bug 还是有意简化？ |

### 4. Plan 服务

| 接口 | 待确认项 |
|------|---------|
| `PlanDetail.phase` / `phase_goal` | proto 定义了 `phase`（field 16）和 `phase_goal`（field 17），但 service 层是否已填充？ |
| `PlanDetail.phase_blueprint_summary` | proto 未定义此字段。是否需要新增？还是前端从 tasks 推导？ |
| `TaskDetail.source` / `reason` | proto 未定义这些字段。是否需要在任务创建时记录来源信息？ |

### 5. Community 服务

| 接口 | 待确认项 |
|------|---------|
| `PostDetail.is_liked` / `is_author` | proto 定义了这两个字段，但 service 层是否已实现查询逻辑？ |
| `Comment.is_author` | proto 定义了 `is_author`，但 service 层是否已填充？ |
| `PostSummary.is_pinned` / `is_recommended` | proto 未定义这两个字段。确认单体是否有此功能？ |

### 6. Membership 服务

| 接口 | 待确认项 |
|------|---------|
| `MembershipStatus` 缺失字段 | `daily_practice_limit`、`daily_interview_limit`、`practice_used_today`、`interview_used_today` 均缺失。确认这些字段是硬编码配置还是需要动态查询？ |
| `MembershipPlan.original_price` / `is_popular` | proto 未定义。确认前端是否使用这些字段？ |

---

## 二、修复建议汇总（按优先级排序）

### P0 — 必须立即修复（核心页面白屏/功能不可用）

| # | 服务 | 问题 | 修复方案 |
|---|------|------|---------|
| 1 | Question | `GetQuestion` 缺 `is_favorited` | service 层查询 `user_favorites` 表填充 |
| 2 | Question | `GetQuestion` 缺 `user_note` | service 层查询 `user_notes` 表填充，或新增 proto 字段 |
| 3 | Question | `SubmitAnswer` 缺 `evaluation_mode` | proto 增加字段，从题目信息获取 |
| 4 | Question | `SubmitAnswer`/`RunCode` 缺 `judge_summary` | proto 增加 `JudgeSummary` 消息，包含 test case 详情 |
| 5 | Question | `ToggleFavorite` 无返回值 | 改为返回 `{is_favorited: bool}` |
| 6 | Question | `GetPracticeRecommendations` 题目不完整 | 嵌入完整 `QuestionDetail` 而非 `QuestionSummary` |
| 7 | Interview | `CreateInterview` 缺 `live2d_directive` | 修改 `toProtoQuestion` 映射 |
| 8 | Interview | `FinishInterview` 仅返回 id+status | 前端需轮询 `GetReport`，或改为同步生成 |
| 9 | Interview | `SubmitAnswer` 缺 `is_finished` | service 层计算并填充 |
| 10 | Growth | `weekly_stats` 始终为空 | 实现按周聚合逻辑 |
| 11 | Growth | `recent_study_logs` 始终为空 | 查询 `study_logs` 表最近 N 条 |
| 12 | Growth | `recent_interviews` 始终为空 | 调用 Interview 服务 `ListInterviews` |
| 13 | Growth | `recent_plans` 始终为空 | 调用 Plan 服务 `ListPlans` |
| 14 | Growth | `practice_stats.category_stats` 始终为空 | 从 Question 服务获取 |
| 15 | Growth | `SyncStudyLog` 结构完全不同 | 重新设计 proto `StudyLog` 消息 |
| 16 | Plan | `PlanDetail` 缺 `phase`/`phase_goal` | 确认 proto 字段并填充 |
| 17 | Plan | `PlanDetail` 缺 `phase_blueprint_summary` | 新增 proto 字段或从 tasks 推导 |
| 18 | Gateway | `flattenPageResult` 替换数组键为 `list` | 确认前端读取路径，或修改 Gateway |
| 19 | Gateway | `normalizeInterviewAnswerPayload` 缺 `is_finished` | service 层计算后 Gateway 传递 |
| 20 | Gateway | `normalizeInterviewReportPayload` 重组但数据为空 | 配合 Interview 服务修复 |
| 21 | Gateway | `normalizePracticeRecommendationPayload` 题目不完整 | 配合 Question 服务修复 |
| 22 | Gateway | `omitempty` 丢失 `is_favorited: false` | 添加 normalizer 补偿 |
| 23 | Gateway | `omitempty` 丢失 `is_liked: false` | 添加 normalizer 补偿 |
| 24 | Gateway | `omitempty` 丢失 `is_author: false` | 添加 normalizer 补偿 |
| 25 | Gateway | `omitempty` 丢失 `is_active: false` | 添加 normalizer 补偿 |
| 26 | Question | `SubmitExam.QuestionResult` 缺 `correct_answer` | service 层映射 `CorrectAnswer` |

### P1 — 尽快修复（功能可用但数据严重不完整）

| # | 服务 | 问题 | 修复方案 |
|---|------|------|---------|
| 27 | Question | `ListQuestions` 缺 `category_id`/`industry_id` | proto 增加字段 |
| 28 | Question | `ListQuestions` 缺 `tags` | proto 增加字段 |
| 29 | Question | `GetQuestion` 缺 `industry_name` | 关联查询 |
| 30 | Question | `GetPracticeStats` 缺 `streak_days`/`today_count` | 查询当天记录 |
| 31 | Question | `WrongQuestion.last_answer` 始终为空 | 修改 SQL 查询 |
| 32 | Question | `CategoryNode` 缺 `question_count` | 聚合查询 |
| 33 | Interview | `GetInterview` 缺 `live2d_model_key` | proto 增加字段 |
| 34 | Interview | `ListInterviews` 缺 `industry_code`/`interview_mode` | proto 增加字段 |
| 35 | Interview | `GetReport` 缺 `duration_seconds`/`completed_at` | 计算并存储 |
| 36 | Interview | `InterviewStats` 缺 `total_questions_answered`/`avg_accuracy` | 增加 SQL 聚合 |
| 37 | Growth | `completed_interview_count` 不准确 | SQL 区分 completed 状态 |
| 38 | Growth | `current_plan` 缺 `id`/`status`/`next_task_*` | 映射 Plan 服务返回值 |
| 39 | Growth | `WeeklyFocus.themes` 缺多个字段 | 从 LearningArchive 聚合 |
| 40 | Plan | `PlanDetail` 缺 `adjustment_summaries` | 查询调整记录 |
| 41 | Plan | `TaskDetail` 缺 `source`/`reason`/`due_date` | 任务创建时记录 |
| 42 | Community | `is_liked`/`is_author` 未填充 | 查询交互表 |
| 43 | Community | `is_pinned`/`is_recommended` 缺失 | 增加帖子表字段 |
| 44 | Membership | `daily_practice_limit` 等字段缺失 | 增加 proto 字段 |
| 45 | Membership | `original_price`/`is_popular` 缺失 | 增加 proto 字段 |
| 46 | Gateway | `unwrapSingleListField` 丢弃外层键 | 确认前端兼容性 |

### P2 — 体验优化（非核心功能）

| # | 服务 | 问题 | 修复方案 |
|---|------|------|---------|
| 47 | Question | `ListQuestions` 缺 `created_at` | 增加字段 |
| 48 | Question | `ListQuestionSets` 缺 `difficulty`/`cover_image` | 增加字段 |
| 49 | Interview | `score` 类型 int32 丢失精度 | 改为 float/double |
| 50 | Growth | `focus_signals` 缺 `topic_code` 等详情 | LearningArchive 增强 |
| 51 | Plan | `ListPlans` 缺 `duration_days` | 增加字段 |
| 52 | Community | `DeletePost` 无返回值 | 返回 `{id}` |
| 53 | Membership | `expires_at` 字段名不一致 | 统一命名 |
| 54 | Gateway | 时间戳转换启发式可能误判 | 改用 proto-aware 序列化 |

---

## 三、修复优先级路线图

### 第一阶段（1-2 天）— 解决白屏问题
1. Growth 服务：填充 `weekly_stats`、`recent_*` 数组、`category_stats`
2. Question 服务：`GetQuestion` 填充 `is_favorited`、`user_note`
3. Interview 服务：`FinishInterview` 前端改为轮询 `GetReport`
4. Gateway：修复 `flattenPageResult` 的数组键名问题

### 第二阶段（3-5 天）— 补齐核心字段
1. Question 服务：`SubmitAnswer` 增加 `evaluation_mode`、`judge_summary`
2. Interview 服务：`CreateInterview` 映射 `live2d_directive`
3. Plan 服务：`PlanDetail` 填充 `phase`、`phase_blueprint_summary`
4. Community 服务：填充 `is_liked`、`is_author`
5. Membership 服务：增加限制次数字段

### 第三阶段（1 周）— 体验优化
1. 补齐所有 P2 字段
2. 为缺失 normalizer 的端点添加 Gateway 补偿
3. 统一枚举值/命名风格

---

# 十、核心业务逻辑对比


## 总体评估

微服务重构在**业务流程主干**上基本保持一致（创建→处理→返回），但在以下方面存在系统性差异：

| 维度 | 评估 |
|------|------|
| 数据查询 | 微服务通过 gRPC 跨服务调用替代单体本地 DB 查询，数据丰富度下降 |
| 评分/判分 | 单体有本地确定性判分+AI 双通道，微服务纯依赖 AI |
| 事务保证 | 微服务在关键路径使用显式事务，优于单体 |
| 异步处理 | 单体支持同步+异步双路径，微服务多为纯异步 |
| 用户状态 | 单体在 service 层填充 is_favorited/is_liked/is_author，微服务缺失 |
| 聚合统计 | 单体直接查 DB 聚合，微服务跨服务聚合但下游数据不足 |

---

## 一、Question 服务

### 1.1 SubmitAnswer — 评分机制完全不同

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 单选/多选题 | **本地确定性判分** (`judgeAnswer`) | **纯 AI 判分** |
| 编程题 | 测试用例逐个运行判分 (`EvaluationModeTestcase`) | 纯 AI 判分 |
| AI 调用 | `quizAnalyzer.AnalyzeCode()` 返回 `CodeAnalysis` | `quizAnalyzer.Analyze()` 返回 `QuizAnalyzerResponse` |
| AI 降级 | 有 fallback 分析逻辑 | 直接返回 InternalServer 错误 |
| 记录方式 | `Upsert`（按 user+question 去重） | `Create`（每次新建记录） |
| 学习档案同步 | ✅ `syncPracticeLearningArchive` | ❌ 缺失 |

**影响**: 单体的本地判分保证了选择题的确定性结果（100%准确），微服务纯依赖 AI 可能出现判分不一致。

### 1.2 GetWrongQuestions — "错题"定义不同

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 定义 | 取每道题**最新一次**答题记录，最新答对则移除 | 取**所有** `is_correct=false` 的记录聚合 |
| 排序 | `created_at DESC`（最新答错排前） | `wrong_count DESC`（错得最多排前） |
| "已修正" | ✅ 最新答对则从错题本移除 | ❌ 历史错误永远保留 |

**影响**: 用户答对后错题本仍显示该题，体验不一致。

### 1.3 GetPracticeRecommendations — 推荐算法完全不同

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 数据来源 | `LearningArchive` 中的 `MistakeTags` 统计高频错因 | `WrongQuestions` 错题记录 |
| 算法 | 错因标签→关键词扩展→题库搜索 | 加权评分（错题次数×1.0，面试×2.0，难度 hard×1.5） |
| 题型筛选 | 仅推荐编程题 (`TypeCode`) | 不限制题型 |
| 默认数量 | 6 道 | 10 道 |
| 返回丰富度 | 含 TopicCode, TopicProblemPattern, RelatedQuestionSets 等 | 仅含 RecommendReason, FocusTag, TopicTitle |

### 1.4 ListQuestionSets — 数据来源完全不同

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 数据来源 | 内存匹配预定义 `questionSetDefinition`（关键词+标签） | 查询独立 `question_sets` 数据库表 |
| Slug | 预定义固定值 | `slugify(Name)` 动态生成 |
| 预览题目 | 每个题单最多 4 道 | 展示关联的所有题目 |

### 1.5 SubmitExam — 存储和评分均不同

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 存储 | 内存 `sync.Map`（重启丢失） | 数据库持久化 |
| 评分 | 本地确定性判分 (`judgeAnswer`) | 纯 AI 评分 |
| 总分计算 | `correctCount / totalCount * 100` 百分制 | 累加各题 AI Score |
| 已完成检查 | 无（内存会话用完即删） | 检查 `exam.Status == "completed"` |

### 1.6 ListQuestions — 过滤能力缺失

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 过滤条件 | `IndustryID`, `CategoryID`, `Difficulty`, `Keyword`, `Type`, `Tags`, `IsActive` | `IndustryCode`, `CategoryID`, `Difficulty`, `Keyword` |
| 排序 | `ORDER BY id DESC` | 无显式排序 |
| 分页上限 | `pageSize` 上限 100 | 无上限 |

### 1.7 GetQuestion — 缺少用户态数据

| 维度 | 单体 | 微服务 |
|------|------|--------|
| `is_favorited` | ✅ 查询 `user_favorites` 表 | ❌ 缺失 |
| `user_note` | ✅ 查询 `user_notes` 表 | ❌ 缺失 |
| `TagList` | ✅ 解析标准化 | ❌ 返回原始 JSON 字符串 |
| `Solution`/`JudgeConfig` | ✅ 结构化对象 | ⚠️ JSON 字符串（Gateway 解析） |

---

## 二、Interview 服务

### 2.1 SubmitAnswer — 即时反馈差异

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 即时 AI 反馈 | ❌ `Feedback` 始终为 nil | ✅ 返回 score, is_correct, feedback, key_points, suggestions |
| 评估时机 | 延迟到 `FinishInterview` 补评 | 立即评估 |
| 编程题处理 | 嵌入 SubmitAnswer，仅持久化不执行 | 独立 `SubmitCodingAnswer`，执行代码+AI 评分 |
| 事务 | 无显式事务 | 单一事务（用户答案+AI反馈+下一题+更新index） |
| 学习档案 | ❌ 不在此处写入 | ✅ 异步写入 |

**影响**: 这是用户体验上的显著差异。微服务的即时反馈更好，但单体的延迟评估可能更准确（整体上下文更完整）。

### 2.2 FinishInterview — 同步 vs 异步

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 报告生成 | 支持同步+异步双路径 | **始终异步**（MQ） |
| 返回内容 | 同步路径返回完整报告 | 仅返回 `interview_id` + `status: "generating"` |
| AI 策略 | `interviewAgent.GenerateReport(sessionID)` 一次性生成 | 逐题评估后汇总 |
| 报告存储 | `interview.ReportJSON` 字段 | 独立 `interview_reports` 表 |
| 幂等性 | 无 | `ON CONFLICT` 保证幂等 |

### 2.3 GetReport — correct_count 计算不同

| 维度 | 单体 | 微服务 |
|------|------|--------|
| correct_count 来源 | AI 判定（`interviewReportPayload.CorrectCount`） | **本地规则估算** |
| 估算逻辑 | — | `EstimateAnswerScore`: 空回答=0, <20字=45, <80字=62, <200字=75, ≥200字=84 |
| 判定标准 | AI 基于回答质量判断 | 按回答长度估算，≥60 算正确 |

**影响**: 一个 200 字的错误回答也会被计为正确（84≥60）。这是**准确性风险**。

### 2.4 CreateInterview — 缺失用户薄弱主题

| 维度 | 单体 | 微服务 |
|------|------|--------|
| AI 上下文 | 包含 `UserWeakTopics`（从学习档案提取 Top20 薄弱主题） | 不包含 |
| AI 接口 | 有状态 `InterviewAgent`（内存 session） | 无状态 gRPC |
| 事务 | 分步写入 | 单一事务（面试记录+首题消息） |

### 2.5 状态机差异

| 单体状态 | 微服务状态 |
|---------|-----------|
| `preparing` → `ongoing` → `report_generating` → `completed` | `ongoing` → `report_generating` → `completed` |
| 支持 `cancelled` | 支持 `report_failed` |
| 有 `preparing`（实时面试） | 无 `preparing` |

---

## 三、Growth 服务

### 3.1 GetGrowthSummary — 4 个区块完全为空

| 区块 | 单体 | 微服务 | 原因 |
|------|------|--------|------|
| `weekly_stats` | 按周聚合统计 | ❌ 始终空数组 | 未实现按周聚合逻辑 |
| `recent_study_logs` | 最近 7 条日志 | ❌ 始终空数组 | 未调用 study_log 查询 |
| `recent_interviews` | 最近 5 条面试 | ❌ 始终空数组 | 未调用 Interview 服务 ListInterviews |
| `recent_plans` | 最近 5 条计划 | ❌ 始终空数组 | 未调用 Plan 服务 ListPlans |
| `practice_stats.category_stats` | 分类统计数组 | ❌ 始终空数组 | Question 服务返回了但 Growth 未映射 |
| `practice_stats.today_count` | 今日练习数 | ❌ 始终为 0 | 未查询当天记录 |
| `current_plan.next_task_*` | 5 个字段全有 | ❌ 全部为空 | Plan 服务未返回这些字段 |

### 3.2 GetWeeklyFocus — 算法简化

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 数据源 | 学习档案(24条) + 面试报告(5条) | FocusSignals + WeakTopics（下游简化数据） |
| 核心算法 | 错因标签统计→面试薄弱项合并→专题信息补充 | 直接使用下游 FocusSignal，无统计聚合 |
| 专题丰富度 | TopicCode, TopicProblemPattern, RelatedQuestionSets, Suggestions 等 | 大部分为空 |
| 推荐理由 | 按来源类型区分（mixed/interview_report/learning_archive），含具体出现次数 | 统一文案 |

### 3.3 SyncStudyLog — 结构性不同

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 请求语义 | **每日学习摘要**（聚合数据） | **单条学习事件**（原子事件） |
| 请求字段 | DateKey, PlanID, Summary, FocusTaskTitle, CompletedCount, SkippedCount, CompletedTitles, SkippedTitles | UserId, Action, RefId, DurationSeconds |
| 存储方式 | 按天 Upsert | 按事件插入 |

---

## 四、Plan 服务

### 4.1 CreatePlan — 同步 vs 异步

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 生成方式 | 默认同步（可选异步） | 始终异步 MQ |
| 返回内容 | 完整计划+任务列表 | 仅 plan_id/status/created_at |
| AI 上下文 | UserProfile + FocusSignals + WeakTopics + GoalDescription | 简化参数（WeakTopics, Level, DurationDays） |
| 暂停旧计划 | ✅ `PauseActivePlans` | ❌ 缺失 |

### 4.2 UpdateTaskStatus — 微服务更好

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 状态机 | 无验证，接受任何状态值 | 显式状态机：`pending→in_progress/completed/skipped`, `in_progress→completed` |
| 事务 | 无 | ✅ 完整事务保护 |
| 计数方式 | 全量查询后手动计数 | COUNT SQL 查询 |
| 回退逻辑 | 无 | `completed→active` 回退 |

### 4.3 AdjustPlan — AI 上下文丰富度差异大

| 维度 | 单体 | 微服务 |
|------|------|--------|
| AI 输入 | PhaseBlueprint + FocusSignals + EntryPhase + ReasonCodes + WeakTopics + GoalDescription | Plan + Tasks + Feedbacks + Reason |
| 任务操作 | 删除全部未完成 + 重建 | 增/删/改 分别操作 |
| 元数据更新 | Title, Description, EndDate, PlanJSON(storedContext) | 仅 TotalTasks, CompletedTasks, Status |
| 阶段蓝图 | 重建 PhaseBlueprint | 无 |

### 4.4 SubmitTaskFeedback — 请求字段完全不同

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 请求字段 | TrainingType, QuestionID, MistakeTags, AttemptCount, TimeSpentSeconds, DifficultySelfAssessment, WrongAnswer, Summary | DifficultyFeeling, FeedbackText, ActualDurationMinutes, ProblemAreas |
| 任务状态检查 | 无 | 必须为 "completed" |

---

## 五、Community 服务

### 5.1 CreateComment — 不更新 comment_count（严重缺陷）

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 评论计数 | ✅ 事务中原子递增 `comment_count` | ❌ 仅 insert 评论，不更新帖子计数 |
| 内容校验 | 验证非空 + 长度≤1000 | 仅验证帖子存在性 |

**影响**: 帖子的评论计数永远为 0。

### 5.2 帖子类型集合不兼容

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 类型值 | `article`, `moment` | `discussion`, `article`, `question` |
| 标题必填 | 仅 `article` 类型 | 所有类型 |
| 摘要截断 | 120 字符 + `...` | 200 字符，无后缀 |

### 5.3 ToggleLike — 并发安全问题

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 返回值 | 事务中重新读取 `like_count` | 基于内存 `post.LikeCount +/- 1` 计算 |
| 负数防护 | `GREATEST(like_count - 1, 0)` | 直接 `+ delta` |
| 删除方式 | `Unscoped()` 硬删除 | 普通 `Delete`（软删除） |

### 5.4 GetPost — 浏览量异步递增

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 递增方式 | 同步 `IncrementPostViews`，手动 `post.ViewCount++` | `go repo.IncrementViewCount(context.Background(), id)` 异步 |
| 返回值准确性 | ✅ 最新值 | ⚠️ 旧值（比实际少 1） |

---

## 六、Membership 服务

### 6.1 数据模型不同

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 会员信息存储 | `user` 表（`membership_level`, `membership_expire_at`） | 独立 `user_memberships` 表 |
| Level 值 | `"pro"` | `planType`（如 `"monthly"`） |
| 会员判断 | `user.IsPro()`（检查 level=="pro" 且未过期） | `membership.Level != "free"` 且未过期 |

### 6.2 ListPlans — 价格不一致

| 方案 | 单体价格 | 微服务价格 |
|------|---------|-----------|
| 月度 | 29.9 | 29.9 |
| 季度 | **69.9** | **79.9** |
| 年度 | **199.9** | **299.9** |

### 6.3 HandlePaymentCallback — 微服务更好

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 幂等性 | ❌ 重复回调报错 | ✅ 已支付订单返回成功 |
| 事务 | ❌ 无 | ✅ 事务保证原子性 |
| ExpiresAt | 回调时计算 | 创建时预设 |

---

## 七、跨服务差异汇总

### 微服务优于单体的方面

| 服务 | 方法 | 改进点 |
|------|------|--------|
| Interview | SubmitAnswer | 返回即时 AI 反馈 |
| Interview | CreateInterview/SubmitAnswer | 事务保证原子性 |
| Interview | GetReport | `ON CONFLICT` 幂等 |
| Plan | UpdateTaskStatus | 显式状态机 + 事务保护 |
| Community | DeletePost | 事务级联删除 |
| Membership | HandlePaymentCallback | 幂等 + 事务 |
| Question | GetWrongQuestions | 支持分页 |
| Growth | GetGrowthSummary | 并发调用 + 降级 |

### 微服务不如单体的方面

| 服务 | 方法 | 退化点 |
|------|------|--------|
| Question | SubmitAnswer | 丢失本地确定性判分 |
| Question | GetQuestion | 缺失 is_favorited/user_note |
| Question | GetWrongQuestions | "错题"定义不同 |
| Question | GetPracticeRecommendations | 推荐算法完全不同 |
| Interview | FinishInterview | 丢失同步报告生成 |
| Interview | GetReport | correct_count 用长度估算替代 AI 判定 |
| Interview | CreateInterview | 缺失用户薄弱主题注入 |
| Growth | GetGrowthSummary | 4 个区块完全为空 |
| Growth | SyncStudyLog | 结构完全不同 |
| Plan | CreatePlan | 丢失同步路径和丰富 AI 上下文 |
| Plan | AdjustPlan | AI 上下文大幅简化 |
| Community | CreateComment | 不更新 comment_count |
| Community | ToggleLike | 并发不安全 |
| Membership | ListPlans | 价格不一致 |

---

## 八、Admin 服务

### 8.1 GetDashboard — 数据聚合方式不同

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 数据来源 | 本地 DB 直接查询（4 个 COUNT + 1 个 Count） | 并发 gRPC 调用 user/question/interview 服务 |
| Today Active Users | 等于 NewUsersToday（简化实现） | 由 User 服务计算（语义可能不同） |
| Interview 统计 | 始终查询 | 仅在 `interviewClient != nil` 时查询 |

### 8.2 AdminListQuestions — 客户端过滤降级

| 维度 | 单体 | 微服务 |
|------|------|--------|
| Category/Industry 过滤 | 服务端 SQL WHERE | 当 `categoryID > 0` 或 `industryCode != ""` 时，**客户端过滤**：批量拉取 200 条/页，内存中匹配 |
| 响应结构 | 解析后的结构化对象（Options, Solution, JudgeConfig） | 原始 JSON 字符串 |

**影响**: 客户端过滤在大数据量下性能差，且分页不准确（过滤后总数≠请求的 total）。

### 8.3 BatchImportQuestions — 缺失分类解析

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 分类解析 | 按 `category_name` 查本地 DB 匹配 | **不解析分类**，仅设置 IndustryID |
| 导入方式 | `BatchCreate` 批量 DB 插入（事务） | 逐条调用 `questionClient.CreateQuestion` gRPC |

**影响**: 导入的题目 `CategoryID = 0`，除非 Question 服务有默认处理。

### 8.4 AI 配置管理 — 预设同步缺失

| 维度 | 单体 | 微服务 |
|------|------|--------|
| UpdateAIConfigs 后 | 同步活跃预设快照 (`syncActiveAIPresetSnapshot`) | ❌ 不同步 |
| DeleteAIPreset | 活跃预设不可删除 | ❌ 无保护，可删除活跃预设 |
| CreateAIPreset | 验证名称唯一性 | 依赖 repo 层 |
| 返回 Support/Warnings | ✅ | ❌ 缺失 |

### 8.5 GenerateQuestionPipeline — 架构完全不同

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 编排方式 | 本地全流程：爬取→清洗→AI生成→去重 | 完全委托给 AI Gateway |
| 爬取 | 调用 ScraperProvider 搜索+抓取 | AI Gateway 处理 |
| 清洗 | 调用 QuestionCleaner | AI Gateway 处理 |
| 约束执行 | 本地验证类型/难度/数量 | AI Gateway 处理 |
| 去重 | 本地去重 | AI Gateway 处理 |
| 统计 | 返回 searched/fetched/scraped/generated 详细计数 | 依赖 AI Gateway 返回 |

### 8.6 ScraperImport — 分类匹配方式不同

| 维度 | 单体 | 微服务 |
|------|------|--------|
| 分类匹配 | 模糊匹配（lowercase + 子串回退） | 精确匹配（`normalizeCategoryName` lowercase+trim） |
| 导入方式 | 批量 DB 插入 | 逐条 gRPC 调用 |

### 8.7 资源引用验证

| 方法 | 单体 | 微服务 |
|------|------|--------|
| CreateQuestion | ✅ 验证 industry/category 存在 | ❌ 委托给 Question 服务 |
| CreateLive2DModel | ✅ 验证 industry/TTS 引用 | ❌ 不验证 |
| CreateTTSConfig | ✅ 验证 engine/voice/auth | ❌ 不验证 |
| CreatePrompt | ✅ 验证 industry 引用 | ❌ 不验证 |

### 8.8 微服务新增功能

| 功能 | 说明 |
|------|------|
| `GetCurrentLive2DModel` | 按场景+行业获取当前 Live2D 模型 |
| `ListSelectableLive2DModels` | 可选模型列表（含 motion 解析） |
| `IndexAllQuestions` / `IndexQuestions` | RAG 索引管理 |
| `SyncRAGDocumentsToVectorDB` | RAG 文档向量同步 |
| `ListScraperTasks` / `RetryScraperTask` | 爬虫任务管理 |
| `UpdateQuestionPipelineTask` | 管线任务状态更新 |
| `ImportLive2DBackground` | Live2D 背景图导入 |

### 8.9 Admin 服务差异汇总

| 判定 | 方法数 |
|------|--------|
| 一致 | 10 |
| 有差异 | 22 |
| 微服务新增 | 7 |
| 微服务缺失 | 0 |

**核心风险**:
1. `BatchImportQuestions` 不解析分类 — 导入数据不完整
2. `UpdateAIConfigs` 不同步预设 — 配置可能不一致
3. `DeleteAIPreset` 无活跃保护 — 可误删关键预设
4. `AdminListQuestions` 客户端过滤 — 大数据量性能差
5. `GenerateQuestionPipeline` 完全依赖 AI Gateway — 行为不可控
