 MakeJob 数据流分析报告

  核心中枢：learning_archive_entries 表

  整个系统的数据流围绕一个核心表展开——learning_archive_entries（学习档案条目）。它采用统一的 mistake_tags_json /
  strength_tags_json / suggestions_json 结构，通过 source_type 区分三个写入来源：

  ┌────────────────────┬────────────────────────────────────────────────┬────────────────────────────────────┐
  │    source_type     │                     写入方                     │              触发时机              │
  ├────────────────────┼────────────────────────────────────────────────┼────────────────────────────────────┤
  │ practice_question  │ questionService.syncPracticeLearningArchive    │ 用户提交编程题/主观题答案后        │
  ├────────────────────┼────────────────────────────────────────────────┼────────────────────────────────────┤
  │ interview_coding   │ interviewService.persistLearningArchiveEntries │ 面试结束生成报告时                 │
  ├────────────────────┼────────────────────────────────────────────────┼────────────────────────────────────┤
  │ plan_task_feedback │ planService.persistTaskFeedbackDiagnosis       │ 学习计划任务提交训练反馈后（异步） │
  └────────────────────┴────────────────────────────────────────────────┴────────────────────────────────────┘

  ---
  问题 1：刷题记录如何影响学习计划和成长档案

  数据写入路径：

  1. 用户提交答案 → questionService.SubmitAnswer (question_service.go:475)
  2. 判分后，对编程题/主观题调用 quizAnalyzer.AnalyzeCode 获取 AI 分析（含 MistakeTags、StrengthTags、Improvements）
  3. 保存答题记录到 user_question_records 表（recordRepo.Upsert，line 552）
  4. 关键桥接：调用 syncPracticeLearningArchive（line 556），将 AI 分析的错因标签写入
  learning_archive_entries，source_type = "practice_question"

  数据消费路径：

  - 成长档案：growthService.GetGrowthSummary（growth_service.go:234）直接查询 learningArchiveRepo.ListRecentByUser
  获取最近 24 条档案条目，加上最近 5 场面试，调用 buildTrainingFocusSignals（focus_signal_support.go:45）聚合出 Top 3
  训练重点信号，返回 focus_signals 和 trend_summary
  - 练习推荐：questionService.GetPracticeRecommendations（question_service.go:1027）同样读取
  learning_archive_entries，提取高频错因标签，扩展为关键词在题库中检索匹配题目
  - 学习计划生成：planService.GeneratePlan（plan_service.go:170）调用 buildLatestPlanFocusSignals（line
  1340）读取档案+面试，生成 focusSignals，合并到 UserProfile.WeakTopics 中传给 AI

  关键数据结构：
  - model.LearningArchiveEntry：MistakeTagsJSON（错因标签 JSON）、StrengthTagsJSON、SuggestionsJSON
  - trainingFocusSignal（focus_signal_support.go:15）：统一的聚合信号结构，包含 Tag、OccurrenceCount、Source 等

  结论：刷题数据通过 learning_archive_entries 中间表实现跨模块共享，不直接查询 user_question_records。

  ---
  问题 2：面试评价如何同步到成长档案

  数据写入路径：

  1. 面试结束 → interviewService.FinishInterview（interview_service.go:488）
  2. 调用 interviewAgent.GenerateReport 生成 ai.InterviewReport（含 Weaknesses、Suggestions、CodingDiagnostics）
  3. 对编程题调用 enrichInterviewReportWithCoding（line 531），通过 quizAnalyzer.DiagnoseInterviewCoding 生成每题的
  CodingQuestionDiagnosis（含 MistakeTags、StrengthTags）
  4. 调用 persistLearningArchiveEntries（line 540，interview_service.go:845），将每道编程题的诊断结果写入
  learning_archive_entries，source_type = "interview_coding"
  5. 完整报告 JSON 序列化存入 mock_interviews.report_json 字段

  数据消费路径：

  - 成长档案：growthService.GetGrowthSummary 同时读取 learning_archive_entries（练习+计划反馈）和
  mock_interviews（面试报告），在 buildTrainingFocusSignals 中合并聚合。面试报告的弱点通过
  collectInterviewFocusSignalSeeds（focus_signal_support.go:125）解析 report.Weaknesses 提取
  - 本周重点：growthService.GetWeeklyFocus（growth_service.go:363）做同样的聚合，输出 Top 3 补强主题

  面试报告存储形式： mock_interviews.report_json 存储完整 JSON（ai.InterviewReport 结构），包含
  overall_score、weaknesses[]、suggestions[]、coding_diagnostics[] 等。不是结构化表，是单一 JSON 字段。

  结论：面试结果通过两条路径同步——编程题诊断写入 learning_archive_entries（结构化标签），完整报告存入
  mock_interviews.report_json（JSON）。成长档案模块同时消费两者。

  ---
  问题 3：学习计划生成时如何参考用户历史数据

  GeneratePlan 流程（plan_service.go:170）：

  1. 调用 buildLatestPlanFocusSignals（line 1340）：
    - 从 learningArchiveRepo.ListRecentByUser 获取最近 24 条档案，按 industry_code 过滤
    - 从 interviewRepo.ListByUser 获取最近 5 场面试，按 industry_id 过滤
    - 调用 buildTrainingFocusSignals 聚合出 Top 3 训练重点信号
  2. 调用 mergePlanProfileWeakTopics（line 1401）：将用户手动填写的 weak_topics 与系统识别的 focusSignals.Tag 合并
  3. 构建 ai.UserProfile，其中 WeakTopics 包含合并后的弱项列表
  4. 传给 planAgent.GeneratePlan，AI prompt 中包含 薄弱主题: xxx

  AdjustPlan 流程（plan_service.go:498）：

  1. 调用 loadPlanAdjustmentDecision（plan_adjustment_support.go:42）：
    - 从 diagnosisRepo.ListRecentByPlan 获取最近 8 条任务诊断
    - 解析 ActionJSON 中的动作建议（如 add_review_task、repeat_same_pattern）
    - 按任务类型计算表现分（buildDiagnosisPerformanceScore：improved=92, partial=76, unresolved=58）
  2. 构建 ai.PlanAdjustmentInput，包含 Performance、ActionSummaries、ReasonCodes、PhaseBlueprint、WeakTopics
  3. 传给 planAgent.AdjustPlan，AI prompt 中包含阶段蓝图、原因码约束等

  数据来源：实时查询多个表（learning_archive_entries、mock_interviews、learning_task_diagnoses），没有预先计算的用户画像
  缓存。

  ---
  问题 4：AI 陪伴模块是否了解用户当前的学习状态

  分析 companionService.Chat（companion_service.go:74）：

  陪伴服务的依赖注入中没有 LearningArchiveRepository、InterviewRepository 或任何用户状态数据源。它只依赖：
  - companionAgent（AI 对话）
  - live2dDirective（Live2D 指令）
  - ttsSceneService / ttsProvider（语音合成）

  上下文来源： normalizeCompanionMessages（line 178）调用 buildCompanionContextMessage（line
  227），该函数处理的是前端通过 req.Context 传入的
  map[string]any。也就是说，用户学习状态上下文完全依赖前端传入，后端不主动查询。

  buildCompanionContextMessage 将前端传入的 context 整理为系统消息：
  当前学习陪伴上下文：
  - key1: value1
  - key2: value2

  结论：陪伴模块不从后端数据库获取用户学习状态。如果前端传入了薄弱知识点、最近面试结果等信息，陪伴对话可以感知；但后端没
  有主动注入这些数据的逻辑。这是一个潜在的数据孤岛。

  ---
  问题 5：AI 面试出题如何利用题库和成长档案

  分析 interviewService.CreateInterview（interview_service.go:149）和
  providerInterviewAgent.generateQuestion（runtime/interview_agent.go:277）：

  1. 创建面试时，前端传入 IndustryCode、Difficulty、Topics、QuestionCount
  2. AI 出题的 prompt（buildQuestionUserPrompt，runtime/interview_agent.go:401）只包含：
    - 行业、难度、候选主题
    - 已出题列表（避免重复）
  3. 没有传入用户的刷题历史、成长档案、上次面试弱点等信息

  InterviewConfig 结构只有 IndustryCode、Difficulty、Topics、QuestionCount 四个字段，没有用户画像字段。

  结论：面试出题不参考用户历史数据。题库与用户画像之间没有匹配逻辑。出题完全基于用户在创建面试时手动选择的主题和难度。

  ---
  汇总数据流示意图

  ┌─────────────────────────────────────────────────────────────────────┐
  │                        用户操作层                                    │
  │  刷题(SubmitAnswer)  面试(FinishInterview)  计划任务反馈(SubmitTaskFeedback) │
  └───────┬──────────────────┬──────────────────────┬───────────────────┘
          │                  │                      │
          ▼                  ▼                      ▼
  ┌───────────────┐  ┌────────────────┐  ┌──────────────────────┐
  │ user_question │  │ mock_interviews│  │ learning_task_feedback│
  │ _records      │  │ .report_json   │  │ (结构化训练反馈)       │
  │ (答题记录)     │  │ (面试报告JSON)  │  └──────────┬───────────┘
  └───────────────┘  └───────┬────────┘             │
          │                  │               异步诊断(15s超时)
          │                  │                      │
          │    编程题诊断      │     AI诊断           │
          │                  ▼                      ▼
          │         ┌─────────────────────────────────────┐
          └────────►│     learning_archive_entries        │◄─┘
                    │  (统一学习档案 - 核心中枢)            │
                    │  source_type: practice_question /   │
                    │               interview_coding /    │
                    │               plan_task_feedback    │
                    │  mistake_tags_json / strength_tags   │
                    │  / suggestions_json                 │
                    └──────────┬──────────────────────────┘
                               │
                ┌──────────────┼──────────────────┐
                ▼              ▼                  ▼
      ┌─────────────┐ ┌──────────────┐  ┌──────────────────┐
      │ 成长档案页   │ │ 练习推荐      │  │ 学习计划生成/调整  │
      │ GetGrowth   │ │ GetPractice  │  │ GeneratePlan     │
      │ Summary     │ │ Recommendat  │  │ AdjustPlan       │
      │             │ │ ions         │  │                  │
      │ 聚合:       │ │ 聚合:        │  │ 聚合:             │
      │ archive +   │ │ archive      │  │ archive +        │
      │ interviews  │ │ →关键词检索   │  │ interviews +     │
      │ →focus      │ │ →题库匹配    │  │ diagnoses        │
      │  signals    │ │              │  │ →UserProfile     │
      └─────────────┘ └──────────────┘  │ →PlanAdjustInput │
                                        └──────────────────┘

      ┌──────────────────┐
      │ AI 陪伴模块       │  ← 不读取后端数据，上下文完全依赖前端传入
      │ companionService │     (潜在数据孤岛)
      └──────────────────┘

      ┌──────────────────┐
      │ AI 面试出题       │  ← 不参考用户历史，仅基于用户选择的主题+难度
      │ interviewAgent   │     (潜在数据孤岛)
      │ .generateQuestion│
      └──────────────────┘

  ---
  发现的问题

  1. 数据孤岛：陪伴模块不了解用户学习状态
  - companionService 没有注入任何用户状态数据源
  - 如果前端未传入 context，陪伴对话完全不知道用户的薄弱点、最近面试表现
  - 建议：在 companionService.Chat 中主动查询 learning_archive_entries 和最近面试摘要，注入到 system prompt

  2. 数据孤岛：面试出题不参考用户画像
  - InterviewAgent.StartInterview 的 prompt 中没有用户历史数据
  - 用户的高频错因标签、上次面试弱点等信息未被用于个性化出题
  - 建议：在 CreateInterview 时查询 learning_archive_entries 和上次面试报告，将薄弱主题注入到出题 prompt

  3. 重复查询：成长档案和练习推荐各自独立查询 learning_archive_entries
  - GetGrowthSummary、GetWeeklyFocus、GetPracticeRecommendations、GeneratePlan 都各自调用 ListRecentByUser
  - 没有缓存层，每次请求都实时查询数据库
  - 影响：目前可接受（数据量小），但随用户增长可能成为瓶颈

  4. 面试报告仅编程题进入学习档案
  - persistLearningArchiveEntries（interview_service.go:845）只在 len(report.CodingDiagnostics) > 0 时写入
  - 非编程题（技术问答、行为题）的弱点只存在于 report_json 的 weaknesses[] 中，不进入结构化的 learning_archive_entries
  - 成长档案通过 collectInterviewFocusSignalSeeds 解析 report.Weaknesses 做了补偿，但这依赖 JSON 解析而非结构化查询

  5. 学习计划调 plan 只看 diagnosis，不直接看 archive
  - loadPlanAdjustmentDecision 只查询 learning_task_diagnoses 表
  - 而 GeneratePlan 查询的是 learning_archive_entries + mock_interviews
  - 两条路径的数据源不完全一致，可能导致生成和调整时对用户弱点的判断有偏差


  claude --resume ab12a0cb-9b14-4b3d-b82e-6319cd6e7a31