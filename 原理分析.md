# LLM 控制 Live2D 动作 —— 原理分析

## 一、项目概览

本项目是一个**纯前端** React 应用，核心思路是：让大语言模型（LLM）在回复用户消息的同时，以结构化 JSON 的形式输出 Live2D 角色的表情和参数指令，前端解析后驱动 Live2D 模型做出对应的动作。

灵感来源于 Neuro Sama 和 Airi 社区项目。

### 技术栈

| 层级 | 技术 |
|------|------|
| 框架 | React 19 + TypeScript + Vite |
| 2D 渲染 | PixiJS 6 |
| Live2D | pixi-live2d-display 0.4.0 (Cubism 4 SDK) |
| LLM 调用 | 浏览器直接 fetch，兼容 OpenAI Chat Completions API |
| TTS | Qwen DashScope / 浏览器原生 TTS / 通用 TTS API |
| 样式 | Tailwind CSS 4 |

**没有后端服务器。** 浏览器直接调用 LLM API 和 TTS API。

---

## 二、核心原理：结构化 JSON 输出

### 2.1 LLM 返回的 JSON Schema

LLM 被要求返回严格 JSON，包含以下字段：

```json
{
  "reply": "回复用户的文本",
  "expression": "gaming",
  "expressionMix": [
    { "expression": "gaming", "weight": 1.0 },
    { "expression": "starry_eyes", "weight": 0.8 }
  ],
  "parameterOverrides": [
    { "id": "ParamMouthOpenY", "value": 0.28 },
    { "id": "ParamMouthForm", "value": 0.45 }
  ],
  "intensity": 0.9,
  "durationMs": 3000
}
```

| 字段 | 作用 |
|------|------|
| `reply` | 展示给用户的文本回复 |
| `expression` | 主表情 ID（向后兼容） |
| `expressionMix` | 多表情加权混合层（最多 3 层） |
| `parameterOverrides` | 直接覆盖 Live2D 参数（如嘴巴、眉毛） |
| `intensity` | 整体表情强度 |
| `durationMs` | 表情持续时间 |

### 2.2 为什么用 JSON 而不是标签？

传统做法是在回复文本中嵌入类似 `[动作:开心]` 的标签。本项目选择结构化 JSON 的优势：

1. **解析可靠性高** —— JSON schema 可以强制 `response_format: { type: "json_object" }`
2. **支持多层混合** —— `expressionMix` 允许同时叠加多个表情并指定权重
3. **支持细粒度参数** —— `parameterOverrides` 可以精确控制嘴巴张合度、眉毛角度等
4. **不易与正文混淆** —— 标签容易被用户看到，JSON 则完全隐藏

---

## 三、完整数据流

```
用户输入
  │
  ▼
┌─────────────────────────────────────────────┐
│  createSystemPrompt()                       │
│  - 注入当前可用表情目录（expression catalog）  │
│  - 注入可用参数白名单（parameter whitelist）   │
│  - 注入当前活跃状态 [avatar_state ...]        │
│  - 注入角色人设（tone, traits, styleRules）   │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  LLM API (OpenAI-compatible)                │
│  POST /chat/completions                     │
│  response_format: { type: "json_object" }   │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  parseAssistantPayload()                    │
│  1. stripMarkdownFence()  去除 ```json 包裹   │
│  2. stripAvatarStateMarkers() 去除状态标记    │
│  3. extractJsonObject() 提取 JSON 子串       │
│  4. JSON.parse()                             │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  normalizeExpressionMix()                   │
│  - 校验 expression ID 是否存在于模型资产       │
│  - 权重 clamp 到 [0, 1]                     │
│  - 合并重复层，保留最多 3 层                   │
│  - 过滤掉 weight < 0.02 的层                 │
│                                             │
│  normalizeParameterOverrides()              │
│  - 校验参数 ID 是否在白名单中                  │
│  - 值 clamp 到参数的 [min, max] 范围          │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  App.tsx 状态更新                             │
│  setActiveExpressionMix(response.expressionMix)     │
│  setActiveParameterOverrides(response.parameterOverrides) │
│  speakText(response.reply)  ──► TTS + 唇形同步     │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  Live2DStage.tsx useEffect 触发              │
│  applyExpressionMix(runtime, avatar, mix)   │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  live2dEngine.ts                            │
│  applyRuntimeState() → buildMixedParameters()│
│  对每个表情层：                                │
│    resolveBinding() 加载 .exp3.json          │
│    mergeBindingIntoParameters() 按模式混合     │
│  最后叠加 parameterOverrides                  │
│  结果写入 overlayTargetParams                 │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────┐
│  PixiJS 每帧渲染循环 (ticker)                │
│  对每个参数：                                  │
│    current = easeTowards(current, target)    │
│    coreModel.setParameterValueById(id, val)  │
│  Live2D 模型平滑过渡到目标状态                  │
└─────────────────────────────────────────────┘
```

---

## 四、关键机制详解

### 4.1 Prompt Engineering —— 动态系统提示词

系统提示词（`createSystemPrompt()` in `llm.ts`）是**动态生成**的，每次调用 LLM 时都会根据当前角色模型注入最新信息。

**表情目录注入：**
```
Only use expressions from this exact catalog:
- id: "neutral", kind: "emotion", label: "Neutral", meaning: "default calm face..."
- id: "starry_eyes", kind: "emotion", label: "Starry Eyes", meaning: "sparkling eyes..."
- id: "microphone", kind: "prop", label: "Microphone", meaning: "holding a mic..."
```

**参数白名单注入：**
```
You may also apply direct parameter overrides from this exact whitelist:
- id: "ParamMouthOpenY", label: "Mouth Open", range: [0, 1]
- id: "ParamCheek", label: "Cheek Flush", range: [0, 1]
```

**表达类型语义：**
- `emotion`（情绪）—— 可混合，支持分数权重，如 0.8 的开心 + 0.3 的害羞
- `pose`（姿势）/ `prop`（道具）/ `effect`（特效）—— 离散开关，权重恒为 1

**状态延续机制：**
每次调用前注入当前状态，让 LLM 知道角色正在做什么：
```
Ongoing avatar control state before the next reply: [avatar_state gaming:emotion:0.85, microphone:prop:1.00]
```
这个标记会被 `stripAvatarStateMarkers()` 从最终回复中清除。

### 4.2 表情资产自动发现

项目不会硬编码表情列表。`avatarManifest.ts` 中的 `discoverAvatarParameterControls()` 会：

1. 读取模型的 `.cdi3.json`（Cubism Display Info）获取参数 ID 和名称
2. 读取模型的 `.vtube.json`（VTube Studio 导出）获取参数 min/max 范围
3. 与硬编码的 `genericParameterCatalog`（17 个通用参数）交叉比对
4. 只暴露模型实际支持的参数

表情文件分类通过 `inferExpressionKind()` 基于文件名正则推断：
- 包含 "pose"/"hand"/"finger_heart" → `pose`
- 包含 "game"/"gaming"/"microphone" → `prop`
- 包含 "flower"/"sparkle"/"effect" → `effect`
- 其他 → `emotion`（默认）

### 4.3 表情混合引擎

`live2dEngine.ts` 中的 `buildMixedParameters()` 负责将多层表情混合为最终参数集：

```
对于 expressionMix 中的每一层：
  1. resolveBinding() 加载对应的 .exp3.json 文件（带缓存）
  2. 根据混合模式（Add / Multiply / Overwrite）将参数值合并
  3. 权重越高的层影响越大

最后叠加 parameterOverrides（直接覆盖，不受权重影响）
```

### 4.4 平滑缓动（Easing）

参数不是瞬间跳变，而是每帧向目标值插值（lerp）：

```typescript
current = current + (target - current) * easingFactor
```

不同参数类别有不同的缓动速度：

| 参数类别 | 趋近速度 | 回归速度 |
|----------|----------|----------|
| 眼球 | 0.24（快） | 0.16 |
| 头部/身体角度 | 0.14 | 0.09（慢，模拟自然摆动） |
| 眉毛/脸颊 | 0.18 | 0.12 |
| 嘴巴 | 0.22 | 0.14 |
| 默认 | 0.16 | 0.10 |

这让角色的动作看起来自然流畅，而不是机械地跳变。

### 4.5 唇形同步（Lip-sync）

唇形同步独立于 LLM 控制，在 `tts.ts` 中实现：

- **有音频数据时**（如 Qwen TTS）：通过 `AudioContext` 的 `AnalyserNode` 读取频域数据，取 2-34 号采样（人声频段）的平均值，映射到 `ParamMouthOpenY`
- **无音频数据时**（浏览器 TTS）：用正弦函数生成合成嘴型动画

唇形数据在 `App.tsx` 中与 LLM 输出的 `parameterOverrides` 合并，TTS 的嘴巴控制会**覆盖** LLM 的嘴巴参数。

### 4.6 错误恢复

如果 LLM 返回的 JSON 解析失败：
1. 系统自动重试一次，使用更低的 temperature（0.2）
2. 注入错误纠正系统消息，明确告诉 LLM 格式要求
3. 如果仍然失败，回退到默认的 neutral 表情

---

## 五、支持的 Live2D 参数

项目内置了一个 `genericParameterCatalog`，包含 17 个常用参数：

| 参数 ID | 说明 | 范围 |
|---------|------|------|
| `ParamAngleX` | 头部左右旋转 | -30 ~ 30 |
| `ParamAngleY` | 头部上下旋转 | -30 ~ 30 |
| `ParamAngleZ` | 头部倾斜 | -30 ~ 30 |
| `ParamBodyAngleX` | 身体左右旋转 | -10 ~ 10 |
| `ParamBodyAngleY` | 身体前后旋转 | -10 ~ 10 |
| `ParamBodyAngleZ` | 身体倾斜 | -10 ~ 10 |
| `ParamEyeLOpen` | 左眼开合 | 0 ~ 1 |
| `ParamEyeROpen` | 右眼开合 | 0 ~ 1 |
| `ParamEyeBallX` | 眼球左右 | -1 ~ 1 |
| `ParamEyeBallY` | 眼球上下 | -1 ~ 1 |
| `ParamBrowLY` | 左眉上下 | -1 ~ 1 |
| `ParamBrowRY` | 右眉上下 | -1 ~ 1 |
| `ParamMouthOpenY` | 嘴巴张合 | 0 ~ 1 |
| `ParamMouthForm` | 嘴型（微笑/撅嘴） | -1 ~ 1 |
| `ParamCheek` | 脸红 | 0 ~ 1 |
| `ParamBreath` | 呼吸 | 0 ~ 1 |
| `ParamHandL` / `ParamHandR` | 左右手 | 0 ~ 1 |

实际可用参数取决于模型资产，项目会自动发现并过滤。

---

## 六、支持的 Live2D 模型

项目在 `avatarManifest.ts` 中预定义了 7 个角色：

| 角色 ID | 名称 | 特点 |
|---------|------|------|
| `yumi` | Yumi | 主要参考角色，17+ 表情文件 |
| `ellen` | Ellen | 猫耳少女 |
| `bingtang` | 冰糖 | VTuber 风格 |
| `strawberryBunny` | 草莓兔 | 含试用/完整资产包解析 |
| `rabbitHole` | Rabbit Hole | 夸张表情风格 |
| `fuxuan` | 符玄 | 星穹铁道角色 |
| `huohuo` | 藿藿 | 星穹铁道角色 |

---

## 七、架构总结

```
┌──────────────────────────────────────────────────────────────┐
│                        浏览器 (纯前端)                        │
│                                                              │
│  ┌──────────┐   JSON    ┌──────────────┐   参数指令   ┌─────────────┐
│  │  Chat UI │ ◄───────► │  LLM API 调用 │ ──────────► │ Live2D 引擎 │
│  └──────────┘           │  (llm.ts)    │             │(live2dEngine│
│       │                 └──────────────┘             │   .ts)      │
│       │                      │                       └──────┬──────┘
│       │                      │ 解析/校验/混合                  │
│       ▼                      ▼                              ▼
│  ┌──────────┐        ┌──────────────┐              ┌─────────────┐
│  │  TTS API │        │ avatarManifest│              │  PixiJS     │
│  │ 唇形同步  │        │ 表情/参数发现  │              │  Canvas 渲染 │
│  └──────────┘        └──────────────┘              └─────────────┘
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**核心设计决策：**

1. **结构化 JSON 输出** —— 而非自由文本标签，解析可靠，支持多层混合
2. **运行时自动发现** —— 从模型资产文件中读取表情和参数，不硬编码
3. **白名单过滤** —— LLM 输出的非法参数/表情被静默丢弃，保证安全
4. **帧级缓动** —— 参数平滑过渡，自然流畅
5. **表情类型语义** —— 情绪可混合，姿势/道具/特效为离散开关
6. **状态延续** —— `[avatar_state]` 标记让 LLM 跨对话轮次维持角色状态

---

## 八、当前项目落地进度补充

当前项目围绕本文原理，已完成以下落地：

1. 前台 Live2D 模型不再直接依据本地文件展示，而是必须经过后台自动补录、人工确认和启用后才能进入前台切换列表。
2. 后台删除模型时会同步处理受管资源目录，避免“删库不删文件”后再次被自动补录回来。
3. 陪伴页与面试页已经统一接入后端下发的 `live2d_directive`，前端优先执行后端结构化结果。
4. 已实现第一阶段能力：`expression_mix`、`parameter_overrides`、`mouth_open`。
5. 已开始实现第二阶段能力：基于模型真实动作白名单的 `motion_key` 动作控制与前端 `motion3.json` 播放。

当前尚未完全对齐参考项目的部分主要包括：

1. 多动作排队与编排。
2. 更完整的跨轮次持续状态协议。
3. 更复杂的动作、表情、参数复合调度策略。
